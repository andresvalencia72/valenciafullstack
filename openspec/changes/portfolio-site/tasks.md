# Tasks: Portfolio Site for Andrés Valencia

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~4000-6000 total across 13 slices; individual slices ~150-400 each after splitting PR3 and PR5 (PR1 scaffolding is the sole exception, likely exceeding 400 due to generated files/lockfile) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR1 -> PR2a -> PR2b -> PR3a -> PR4 -> PR3b -> PR5a -> PR5b -> PR6 -> PR7 -> PR8 -> PR9 -> PR10 -> PR11 (see Suggested Work Units; PR2 was split into PR2a/PR2b post-apply, see "PR2 split decision" below) |
| Delivery strategy | ask-on-risk |
| Chain strategy | feature-branch-chain (user-selected mid-apply: merges deferred to project end, each PR targets its parent branch) |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain (user-selected mid-apply: merges deferred to project end, each PR targets its parent branch)
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 1 | Scaffold + test runner + CI skeleton | PR1 | Base for all; generated files (lockfile, boilerplate) likely need `size:exception` even after chaining |
| 2a | Design system: tokens, fonts, theme, motion, scroll progress | PR2a | Base = PR1. Split from the original "PR2" work unit post-apply — see "PR2 split decision" below |
| 2b | i18n shell: next-intl routing, `app/[locale]` restructure, locale switcher | PR2b | Base = PR2a. Split from the original "PR2" work unit post-apply — see "PR2 split decision" below |
| 3a | Home sections part 1: hero, stack strip, about, skills bento | PR3a | Base = PR2b (needs the `app/[locale]` shell) |
| 4 | Blog MDX + article route | PR4 | Base = PR2b (needs the `app/[locale]` shell) |
| 3b | Home sections part 2: projects, articles list, article filter | PR3b | Base = PR4 (consumes the real MDX loader/article data shipped in PR4; no longer runs before it) |
| 5a | Persistence schema + migrations + db client wiring | PR5a | Base = PR1 |
| 5b | Persistence repository interfaces + implementations | PR5b | Base = PR5a |
| 6 | Contact API + form | PR6 | Base = PR5b |
| 7 | Engagement (views/reactions) | PR7 | Base = PR5b, PR4 (engagement mounts in the PR4 article page and validates slugs against the PR4 article set); independent of PR6 |
| 8 | Search (sync script + FTS API + UI) | PR8 | Base = PR5b, PR4, PR3b |
| 9 | SEO pack (OG, RSS, sitemap) | PR9 | Base = PR4, PR3b |
| 10 | GitHub activity (last slice) | PR10 | Base = PR3b |
| 11 | Hardening: headers, Lighthouse, coverage gate, ADRs | PR11 | Base = all prior |

**Chain-strategy note**: the Base column above was valid under `stacked-to-main`, the strategy in effect through PR1/PR2a/PR2b. **Starting at PR3a, the user switched the chain strategy to `feature-branch-chain` mid-apply**: no PR merges to `main` until project end; each PR targets its immediate predecessor branch instead (linear order: PR1 -> PR2a -> PR2b -> PR3a -> PR4 -> PR3b -> PR5a -> PR5b -> PR6 -> PR7 -> PR8 -> PR9 -> PR10 -> PR11). PR3a's branch (`feat/pr3a-home-core`) targets `feat/pr2b-i18n-shell` accordingly, opened ready (not draft) — under `feature-branch-chain`, only the final tracker PR aggregates everything to `main`; intermediate child PRs are reviewable and mergeable into their parent branch without waiting for `main`.

## Phase 1: Scaffolding & CI (PR1)

- [x] 1.1 `git init`; scaffold Next.js App Router + TS + Tailwind
- [x] 1.2 Install/configure Vitest, Testing Library, Playwright (vitest.config.ts, playwright.config.ts)
  - DoD: also update `openspec/config.yaml` `apply.test_command`, `verify.test_command`, and `verify.build_command` with the real commands
- [x] 1.3 ESLint boundaries plugin enforcing domain/application/infra/ui import rules
- [x] 1.4 `shared/config/env.ts` Zod env schema; fail-fast only on missing `DATABASE_URL` and `VISITOR_HASH_SECRET`; `RESEND_API_KEY`/`GITHUB_TOKEN` optional (persistence: Environment-Only Credentials)
- [x] 1.5 `shared/db/client.ts` Drizzle client + `docker-compose.yml` Postgres
- [x] 1.6 GitHub Actions: unit+integration (against pglite), Playwright e2e against a real **GitHub Actions Postgres service container** (`DATABASE_URL` points at it; this task ships the service container plus an empty seed-step skeleton only — seed steps land with their features, see 5a.2/8.1) — e2e flow list scoped to capabilities implemented to date, reaching the full set — home render, article view, contact submission — by the final slice; coverage gate (global 80% lines+branches over `src/**` and `scripts/**`, excluding `app/` route adapters, config files, generated code, and the declarative/IO-bootstrap files `shared/db/schema.ts` and `shared/db/client.ts`; configured deterministically — always enforced, automatically skips (passes) when zero coverage-eligible files exist under the configured scope, e.g. PR1 scaffolding — no manual flip, no separate PR2 task needed), Lighthouse stub (quality-pipeline: End-to-End Test Gate, Coverage Threshold) — task 1.6 is the single owner of the coverage gate config
- [x] 1.7 README badge placeholders; `docs/adr/0001-0007` stubs from design.md
- [x] 1.8 TDD: full security headers via `next.config` (X-Content-Type-Options, X-Frame-Options, Referrer-Policy, HSTS) plus the full CSP — `script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; object-src 'none'; frame-ancestors 'none'; base-uri 'self'` (see design.md ADR-0007) — unit test on the config or e2e header assertion (security: Security Headers, baseline)

## Phase 2a: Design System (PR2a)

Base = PR1. See "PR2 split decision" below for why Phase 2 (originally
one PR) now ships as PR2a + PR2b.

- [x] 2.1 `shared/ui/tokens.css` light/dark tokens from `design-reference/`
- [x] 2.2 `next/font/local`: Clash Display + General Sans
- [x] 2.3 TDD: theme toggle + no-flash inline script (design-system: Theme Toggle) — ships with hardcoded English labels; PR2b reconnects it to next-intl once the message catalogs land
- [x] 2.5 TDD: scroll progress indicator (design-system: Scroll Progress)
- [x] 2.6 TDD: reveal/tilt/magnetic motion + reduced-motion (design-system: Motion Interactions)
- [x] 2.7 Playwright: no-flash theme assertion — assert (a) the inline theme script precedes any stylesheet/body content in the served HTML source, and (b) with Playwright `addInitScript`-seeded `localStorage`, `document.documentElement.dataset.theme` equals the stored value at `domcontentloaded` (design-system: Theme Toggle) — run against the interim `app/layout.tsx` shim's root path; PR2b repoints it at `/es`

## Phase 2b: i18n Shell (PR2b)

Base = PR2a.

- [x] 2.4 next-intl setup, `app/[locale]` layout, `localePrefix: "always"`, `es` default, `/` and unprefixed paths 308-redirect to `es` (i18n: Locale Routing) — replaces PR2a's temporary `app/layout.tsx`/`app/page.tsx` shim
- [x] 2.8 TDD: locale switcher component — generic behavior only: locale switch + preserves current page (i18n: Locale Persistence on Navigation)
- [x] 2.9 TDD: unit tests for `shared/config/env.ts` Zod schema — fail-fast on missing `DATABASE_URL`/`VISITOR_HASH_SECRET`, optional `RESEND_API_KEY`/`GITHUB_TOKEN` (quality-pipeline: Coverage Threshold; this is the first PR with coverage-eligible files under the gate's configured scope, per task 1.6) — already satisfied by PR1's `env.ts` tests (see "PR1 apply findings"); carried here unchanged, no new code

## Phase 3a: Home Sections Part 1 (PR3a)

- [x] 3a.1 TDD: `features/home/ui` hero, stack strip, about, skills bento sections (home-page: Section Composition — partial)
- [x] 3a.2 TDD: anchor nav smooth scroll (home-page: In-Page Navigation)
- [x] 3a.3 Playwright: responsive layout for hero/about/skills across breakpoints (home-page: Responsive Layout — partial)

## Phase 3b: Home Sections Part 2 & Article Filter (PR3b)

- [x] 3b.1 TDD: `features/home/ui` projects (alternating cards), contact, footer sections (home-page: Section Composition — remainder) — note: PR3b ships the static contact section shell only; PR6 (task 6.4) wires the functional form
- [x] 3b.2 TDD: embedded articles list wired through the `app/` composition root and `shared/content` (never a direct cross-feature import from `home` into `blog`) (home-page: Embedded Article List)
- [x] 3b.3 TDD: filter pills + "All" + empty-state (article-filter: Category Filtering/Reset/Empty Result)
- [x] 3b.4 Playwright: home responsive layout for projects/articles/filter across breakpoints (home-page: Responsive Layout — remainder)

## Phase 4: Blog (PR4)

- [x] 4.1 TDD: `shared/content` MDX loader + canonical Zod frontmatter schema (title, description, date, category, tags?, cover?), covering blog: Frontmatter Validation scenarios (Valid frontmatter, Invalid frontmatter) and Cross-Locale Frontmatter Consistency (build fails on divergent category/date/tags)
- [x] 4.2 TDD: `app/[locale]/blog/[slug]/page.tsx` renders matched-locale MDX (blog: MDX Article Rendering)
- [x] 4.3 TDD: missing-translation fallback notice, no 404 (blog: Missing-Translation Fallback)
- [x] 4.4 TDD: 404 when both locales missing; unprefixed `/blog` 308-redirects to `/es/blog` (i18n middleware rule), and both `/es/blog` and `/en/blog` return 404 (no index route) (blog: No Blog Index Route; i18n: Locale Routing)
- [x] 4.5 TDD: per-route `generateMetadata` canonical + hreflang for blog routes, incl. `x-default` pointing to `es` (seo: Bilingual Sitemap with Hreflang; i18n: SEO Metadata Consistency) — cross-ref 9.3
- [x] 4.6 TDD: locale switcher article awareness — same-slug navigation and fallback-notice handling (i18n: Locale-Matched MDX Resolution scenarios)

## Phase 5a: Persistence Schema, Migrations & DB Client (PR5a)

- [x] 5a.1 `shared/db/schema.ts`: contact_messages, article_views, article_reactions (enum thumbs_up/heart/fire), article_search (description column, category column, per-locale regconfig, `search_vector` weighted title=A/body_text=B/description=C), rate_limits (endpoint, key, window_start, count; PRIMARY KEY(endpoint, key, window_start))
- [x] 5a.2 `drizzle-kit generate`, commit migration; wire migrations into the e2e seed step (persistence: Versioned Schema)
- [x] 5a.3 Verify `shared/db/client.ts` wiring against the finalized schema; document the atomic upsert pattern for `rate_limits`, scoped per endpoint (`INSERT ... ON CONFLICT (endpoint, key, window_start) DO UPDATE SET count = count + 1 RETURNING count`)

## Phase 5b: Persistence Repository Interfaces & Implementations (PR5b)

- [x] 5b.1 TDD: repo interfaces per feature domain, zero drizzle imports (persistence: Domain Repository Interfaces)
- [x] 5b.2 TDD: Drizzle repo impls per feature infrastructure, pglite tests (persistence: Infrastructure Repository Implementations)

## Phase 6: Contact (PR6)

- [x] 6.1 TDD: `app/api/contact/route.ts` Zod validation (name ≤100, email ≤254, message ≤5000, locale enum es/en) — 400 on invalid (contact: Server-Side Input Validation)
- [x] 6.2 TDD: persist + Resend email; HTTP 202 "received, delivery delayed" response on email failure or when `RESEND_API_KEY` is absent (persistence succeeds); HTTP 503 reserved for persistence failure only (contact: Message Persistence Independent of Email Delivery)
- [x] 6.3 TDD: Postgres fixed-window rate limit (`ip_hash`, atomic upsert on `rate_limits`, scoped per endpoint, 3 requests/10min) + honeypot silent reject; check order rate limit -> honeypot -> validation, honeypot hits consume rate-limit budget (contact: Rate Limiting, Spam Mitigation)
- [x] 6.4 TDD: contact form ui, generic-only error responses (contact: No PII Leakage) — wires the functional form into the PR3b static shell (see 3b.1)
- [x] 6.5 Playwright: contact submit flow (quality-pipeline: E2E Test Gate)
- [x] 6.6 TDD: privacy disclosure page + footer link (es/en) (contact: Privacy Disclosure)

## Phase 7: Engagement (PR7)

- [x] 7.1 TDD: hashed visitor key value object — `visitor_hash` HMAC-SHA256(IP+UA) keyed by `VISITOR_HASH_SECRET`, no cookies (engagement: Privacy-Respecting Visitor Identity)
- [x] 7.2 TDD: `app/api/engagement/views/route.ts` insert-if-absent permanent dedupe (UNIQUE(slug, visitor_hash), `onConflictDoNothing`), both first write and repeat view return 204 No Content, slug validated against published article set (404 unknown), rate-limited per `ip_hash` (20 requests/min, security: Rate Limiting on Write/Query Endpoints); on DB unavailability returns HTTP 503 (engagement: View Counting with Permanent Dedupe, Endpoint Validation/Unknown Slug Rejection)
- [x] 7.3 TDD: `app/api/engagement/reactions/route.ts` Zod enum thumbs_up/heart/fire, per-type insert-if-absent dedupe, both first write and duplicate reaction return 204 No Content idempotent, unknown slug 404, rate-limited per `ip_hash` (20 requests/min, security: Rate Limiting on Write/Query Endpoints) (engagement: Reactions with Permanent Dedupe, Endpoint Validation/Unknown Slug Rejection)
- [x] 7.4 TDD: public aggregate counts read, no identifiers exposed, `GET /api/engagement/[slug]` returns `{views, reactions: {thumbs_up, heart, fire}}` cached 60s, rate-limited per `ip_hash` (60 requests/min), unknown slug 404, returns HTTP 503 on DB unavailability (engagement: Public Read of Aggregate Counts, Graceful Degradation When Database Unavailable)
- [x] 7.5 TDD: engagement UI (`features/engagement/ui`): reaction buttons (thumbs_up/heart/fire), counts display, client hook firing exactly one view POST post-hydration, DB-degraded state (hide counts on API error) (engagement: View Counting with Permanent Dedupe — view-once-per-load, Graceful Degradation When Database Unavailable)

## Phase 8: Search (PR8)

- [x] 8.1 TDD: `scripts/sync-search.ts`: parse MDX, strip to body_text, full reconcile (upsert + prune) `article_search` + `search_vector` per-locale regconfig, covering search: Index Sync Full Reconcile scenarios; wire `sync-search` into the CI/deploy workflow (pipeline order: migrate -> sync-search -> deploy promote) and into the Playwright e2e seed step (migrations already wired in 5a.2) so the e2e DB reset runs sync before tests — task 8.1 is the sole owner of sync-search seed wiring
- [x] 8.2 TDD: search route, Zod query validation (incl. `locale` enum es/en) + rate limit (`ip_hash`) (search: Input Validation and Rate Limiting)
- [x] 8.3 TDD: `websearch_to_tsquery`/`ts_rank` ordering with per-locale regconfig, empty-state on no match, DB-degraded rendering (search input hidden/disabled) when Postgres unavailable (search: Full-Text Query, Relevance Ranking, No-Match Handling, Graceful Degradation When Database Unavailable)
- [x] 8.4 TDD: search input + results rendered in place within the articles list (not a dropdown) + empty state, positioned above the filter pills in the home articles section; typing a query visually deactivates any active category pill (reset to none), clearing the query restores "All" (home-page: Embedded Article List — search input; search: Full-Text Query; article-filter: Empty Result Handling)

## Phase 9: SEO (PR9)

- [ ] 9.1 TDD: `app/[locale]/blog/[slug]/opengraph-image.tsx` OG image per article+locale; missing-translation fallback serves the content locale's image (seo: Dynamic OG Images)
- [ ] 9.2 TDD: `app/[locale]/rss.xml/route.ts` per-locale feed, entries only for articles existing in that locale, channel language per feed (seo: RSS Feed)
- [ ] 9.3 TDD: `app/sitemap.ts` bilingual hreflang pairing incl. `x-default` -> `es`, excludes missing-locale slugs (seo: Bilingual Sitemap with Hreflang; i18n: SEO Metadata Consistency) — cross-ref 4.5

## Phase 10: GitHub Activity (PR10)

- [ ] 10.1 TDD: `features/github-activity/infrastructure` GitHub API client with `GITHUB_TOKEN` (optional; absent token follows the same fallback path as an API failure) (github-activity: Server-Side GitHub Data Fetch, Graceful Failure Handling)
- [ ] 10.2 TDD: ISR section, `revalidate: 3600`, non-blocking render (github-activity: ISR Caching, Non-Blocking Render)
- [ ] 10.3 TDD: fallback panel on API error/timeout/missing token (github-activity: Graceful Failure Handling)
- [ ] 10.4 Update home page section-order e2e test to include the github activity section between articles list and contact (home-page: Section Composition)

## Phase 11: Hardening & Verify (PR11)

- [ ] 11.1 TDD: verify the full header set + CSP shipped in PR1 (per design.md ADR-0007 — no further tightening planned) and run Lighthouse only (security: Security Headers — CSP)
- [ ] 11.2 TDD: verify no secrets in client bundle (security: No Client-Side Secrets)
- [ ] 11.3 Verify coverage gate integrity (task 1.6 config, `src/**` + `scripts/**`) and raise exclusions review; wire Lighthouse CI budget (mobile preset: Performance >=90, Accessibility >=95, Best Practices >=95, SEO >=95), update README badges (quality-pipeline)
- [ ] 11.4 Full Playwright pass: locale switch, theme, filter, search, contact, article view, engagement, github activity (quality-pipeline: E2E Test Gate)
- [ ] 11.5 Finalize `docs/adr/0001-0007` rationale from design.md ADR table

## Implementation Notes (Judgment Day residuals)

- Engagement UI feedback: after a reaction POST (always 204), apply optimistic increment client-side and persist a local "reacted" flag in localStorage; scenarios live at component-test level (task 7.5).
- Add `EMAIL_DRIVER` (values: resend|fake, default resend) to the env schema, env matrix, and task 1.4/6.2 — e2e uses fake; keeps the 200 {status:"sent"} happy path testable in CI.
- Contact e2e: rate limits are env-configurable (or rate_limits truncated between contact tests) so the 3/10min limit does not 429 the suite from a single CI IP.
- Honeypot response mirrors whatever the CURRENT success path would return (200 or 202 in degraded email mode), not a fixed literal.
- Verify next-intl's emitted redirect status against the pinned version; if 307, override in middleware or relax spec assertions to permanent-style redirect (307/308) — decide in PR2 and record.
- Add `NEXT_PUBLIC_SITE_URL` via a build-safe validated env path (separate schema importable at build time) for sitemap/RSS/canonical/OG absolute URLs.
- Pin request body contracts before TDD in PR7: POST /api/engagement/views {slug}; POST /api/engagement/reactions {slug, kind}.
- Application-layer orchestration rule (rate limit → validation flow) applies to engagement and search use-cases too, not only contact — keeps that logic coverage-gated.
- GitHub activity: implement as Suspense-streamed section with fetch-level revalidate (3600), not page-level ISR; loading state applies to cache-miss renders.
- Add app/[locale]/privacy/page.tsx to the design folder tree mentally — required by contact spec (task 6.6); unknown locale segments (e.g. /fr/rss.xml) return 404.
- Search results render without date unless added to the response shape — decide in PR8; each engagement write endpoint has its own independent 20/min budget.
- env.ts is coverage-gated src/** code ("config files" exclusion means build/tool configs only); its tests may land with 2.9 as scaffolding exception.
- Task 3b.3 covers the empty-state component at unit level; the full empty-result e2e lands with PR8's search input.
- Proposal wording "atomic increments" is legacy — counts are row aggregates (insert-if-absent), per engagement spec.

### PR1 apply findings (2026-07-05)

- **env.ts/env.public.ts tests landed in PR1, not deferred to 2.9.** Vitest's v8 coverage provider (v4.x) has no `all:false`-style opt-out — it always reports every file matched by `coverage.include`, regardless of whether a test touches it. So an untested-but-eligible `env.ts` would have failed the 80% gate immediately in PR1, contradicting the "auto-pass" guarantee. Rather than fight the tool, PR1 ships full unit tests for `env.ts` and `env.public.ts` (fail-fast on missing `DATABASE_URL`/`VISITOR_HASH_SECRET`, `EMAIL_DRIVER` default/enum, build-safe `NEXT_PUBLIC_SITE_URL` default/validation). **Task 2.9 is now redundant** — treat it as "verify/extend existing env.ts tests" rather than "create from scratch" when PR2 lands. The "auto-pass when zero coverage-eligible files exist" behavior itself was verified independently (`vitest run --coverage --coverage.include="<empty-glob>"` exits 0 with an empty 0/0 report), so the gate mechanism in task 1.6 is correct for genuinely-empty scopes — it just doesn't apply once `env.ts` exists.
- **eslint-plugin-boundaries@6.0.2 capture-scoped selector gotcha**: the legacy tuple selector form `["domain", { captured: { feature: "{{from.captured.feature}}" } }]` silently never matches (in either direction) when used inside `allow`/`disallow` — no error, no warning, just a no-op that makes the whole rule permissive-by-default-disallow (i.e., everything gets rejected, or with `default: allow`, nothing gets caught). The working form is the object/`DependencySelector` wrapper: `{ to: { type: "domain", captured: { feature: "{{from.captured.feature}}" } } }`. Verified empirically (see `eslint.config.mjs` inline comment) with a throwaway fixture feature exercising same-feature-allowed / cross-feature-blocked / layer-blocked cases before removing the fixture. Also required `settings["import/resolver"] = { typescript: true, node: {...} }` — without a resolver, boundaries can't resolve `@/*`-aliased or extensionless relative imports and silently treats every dependency as "unknown" (never checked).
- **Folder tree correction applied**: `create-next-app --src-dir` nests `app/` under `src/app`, but design.md's tree places `app/` at the repo root as a sibling to `src/` (screaming architecture: `app/` = composition root only, `src/` = features + shared). PR1 scaffolds with `--src-dir` for the initial `create-next-app` run (so Tailwind/TS/ESLint defaults are generated normally) then moves `src/app` → `app/` immediately after, before writing any feature code. Path alias `@/*` → `./src/*` is unaffected.
- **`.env.example` could not be created** — the execution sandbox denies writes to any `.env*` path, including example/template files with no real secrets. The full recommended content is documented in `README.md` (Environment variables + Getting started) and in `design.md`'s env matrix; a maintainer with local file access should create `.env.example` from that content in a follow-up commit (or relax the sandbox rule for `.env.example` specifically, since `.gitignore` already special-cases it as safe to commit).

### PR2 split decision (2026-07-05)

The original PR2 (Phase 2, tasks 2.1-2.9, ~1960 hand-written insertions/133 deletions) exceeded the 400-line review budget and was split post-apply into two stacked PRs per user decision (`chain_strategy: stacked-to-main`):

- **PR2a — Design System** (`feat/pr2a-design-system`, base = PR1): tokens, self-hosted fonts, theme (toggle + no-flash script + `use-theme`), motion primitives, scroll progress. Tasks 2.1, 2.2, 2.3, 2.5, 2.6, 2.7. Ships a temporary `app/layout.tsx` + `app/page.tsx` shim (PR1's plain structure, wired with fonts/tokens/theme) since `app/[locale]` belongs to PR2b; `ThemeToggle` ships with hardcoded English labels (no next-intl dependency yet).
- **PR2b — i18n Shell** (`feat/pr2b-i18n-shell`, base = PR2a): next-intl routing/navigation/request config, message catalogs, locale switcher, the Proxy (307->308 upgrade), and the `app/[locale]` restructure that replaces PR2a's interim shim. Tasks 2.4, 2.8, 2.9 (2.9 was already satisfied by PR1's `env.ts` tests — carried here unchanged). `ThemeToggle` is reconnected to next-intl's `useTranslations` once the message catalogs exist.

Both branches are independently green (lint, typecheck, coverage >=80%, `next build`, Playwright). The split boundary sits at the atomic-unit line: further splitting (e.g. separating fonts from tokens, or theme from motion) would produce non-functional, non-independently-reviewable slices. Each PR still lands above the 400-line budget (~1600-1700 lines each, see PR bodies for the exact estimate) — disclosed honestly rather than forcing an artificial third split.

### PR2a/PR2b apply findings (2026-07-05)

Supersedes "PR2 apply findings" from the original (now-closed) PR2 draft — same technical content, reorganized across the two branches:

- **307-vs-308 redirect decision (RESOLVED, PR2b)**: verified against the installed `next-intl@4.13.1` middleware source — it calls `NextResponse.redirect(url)` with no explicit status, and Next.js's own `NextResponse.redirect` defaults to **307** when no status is given (confirmed in `node_modules/next/dist/server/web/spec-extension/response.js`). Since the i18n spec requires a **308** (permanent) for the unprefixed-to-`es` redirect, `proxy.ts` wraps next-intl's middleware output and upgrades any 307 to 308 before returning it, preserving all other headers. Locked in by a unit-tested pure function (`shared/i18n/middleware-rules.ts#upgradeRedirectStatus`) plus an e2e test (`e2e/locale-routing.spec.ts`).
- **`middleware.ts` -> `proxy.ts` rename (Next.js 16, PR2b)**: the pinned Next.js version (16.2.10) deprecates the `middleware` file convention in favor of `proxy`. PR2b ships the file as `proxy.ts` from the start.
- **Real hydration bug found and fixed by the Playwright no-flash test (task 2.7, PR2a)**: the original `useTheme()` implementation read `document.documentElement.dataset.theme` (already mutated by the pre-hydration inline script) via a `useState` lazy initializer, producing a genuine React hydration mismatch. Fixed by always starting client state at the SSR-safe default and syncing the real value in a `useEffect` after mount — the same pattern used by `next-themes`.
- **Theme script placement (PR2a)**: `next/script` with `strategy="beforeInteractive"` inside `<body>`, not a raw `<script>` in `<head>` — Next.js's App Router always injects its own resource hints and stylesheet `<link>` at the very top of `<head>`, ahead of any user-authored head content (framework-internal, not configurable). `beforeInteractive` still guarantees the script runs before hydration and before rendered content.
- **Vitest + Node 25 `localStorage` conflict (PR2a)**: Node's built-in global `localStorage` throws `SecurityError` and shadows jsdom's implementation under Vitest. Fixed via `NODE_OPTIONS=--no-webstorage` on the test npm scripts plus a jsdom `environmentOptions.url`.
- **Vitest + `next-intl/navigation` resolution gotcha (PR2b)**: `next-intl`'s react-client build imports the bare specifier `next/navigation`, which Vite's SSR-external resolver can't resolve since `next` ships no package.json "exports" map. Fixed via `ssr.noExternal: ["next-intl"]` + `resolve.alias["next/navigation"]` pointing at the concrete file.
- **Font loader (`next/font/local`) cannot run under Vitest (PR2a)** — excluded `src/shared/ui/fonts/index.ts` from the coverage `include` scope, same basis as the `shared/db` bootstrap-file exclusion category.
- **Fonts (PR2a)**: Clash Display (500/600/700) and General Sans (400/500/600) from Fontshare, committed as `.woff2` with upstream `LICENSE.txt` files.
- **ThemeToggle i18n coupling discovered during the split**: the original single-PR2 `ThemeToggle` imported `useTranslations` from `next-intl` directly, which would have pulled the i18n runtime into PR2a. PR2a ships a decoupled version with hardcoded English labels; PR2b reconnects it to `next-intl` once the message catalogs exist — this is the one place where the original commits could not be cleanly cherry-picked without a small manual adjustment.
- **Coverage**: PR2a 98.29% stmts/94% branches/100% funcs/98.29% lines (62 tests); PR2b (cumulative) 95.36%/92.42%/95.65%/95.33% (84 tests) — matches the original single-PR2 numbers exactly, confirming no test coverage was lost in the split. `shared/i18n/request.ts` is the only 0%-covered file in PR2b (next-intl RSC wiring + a dynamic `import()`; its one piece of real logic, locale fallback, is extracted into the fully-tested `resolveRequestLocale` pure function).

### PR3a apply findings (2026-07-05)

- **Chain strategy switched mid-apply**: the user changed the chain strategy from `stacked-to-main` to `feature-branch-chain` starting at this PR — see the "Chain-strategy note" above. `feat/pr3a-home-core` targets `feat/pr2b-i18n-shell` (not `main`), opened ready (not draft); no PR in the chain merges until project end.
- **ESLint boundaries gap found and fixed**: the `ui` boundary rule (`eslint.config.mjs`) allowed `domain`/`application`/`shared` but had no same-feature-captured entry for `ui` itself, so intra-feature imports (e.g. `hero-section.tsx` importing its own `social-icon.tsx`) were rejected as "ui may not import ui". This is the first real feature folder (`src/features/home/ui`) to exercise the rule beyond PR1's throwaway fixture, and it surfaced immediately. Fixed by adding a same-feature-captured `{ to: { type: "ui", captured: { feature: "{{from.captured.feature}}" } } }` entry to the `ui` allow-list. Re-verified with a temporary cross-feature fixture (not committed): same-feature `ui -> ui` now passes, cross-feature `ui -> ui` still fails.
- **jsdom does not implement anchor same-document hash navigation**: a unit test asserting that clicking a `SectionNav` link updates `window.location.hash` failed even with a correctly-implemented component — confirmed via an isolated jsdom repro that a dispatched `click` on an `<a href="#x">` never updates `window.location.hash`. Real hash/scroll behavior for `home-page: In-Page Navigation` is verified in `e2e/home-sections.spec.ts` (real browser) instead; the unit test for `SectionNav` covers structure/i18n only (correct hrefs, translated labels, order).
- **Design tokens extended for the first time since PR2a**: `tokens.css` only carried `bg`/`band`/`card`/`ink`/`coral`/`salmon` (exercised by the theme-toggle demo). Hero/about/skills need `ink-soft`, `ink-faint`, `line`, `frame`, `shadow`, `coral-ink` — derived verbatim from `design-reference/`'s JS theme map (`Portfolio Andres Valencia.standalone.dc.html`, `themeVars()`), not invented ad hoc.
- **Skill icon fidelity deviation (documented in `skill-badge.tsx`)**: ships monogram badges instead of `devicon`'s colored icon font. Self-hosting devicon would either pull an external CDN stylesheet (blocked by ADR-0007's `style-src 'self'` CSP) or vendor a large multi-hundred-glyph icon font for eight icons; hand-typed brand SVG paths risk visual inaccuracy. The badge component is isolated to one file so real brand SVGs/devicon assets can replace it later without touching the section components.
- **Stale dev server caught before final e2e verification**: an `npm run dev` process from an earlier, unrelated session was still listening on port 3000. Playwright's `reuseExistingServer: !CI` attached to it instead of starting a fresh `next start` production server, which would have silently validated against dev-mode behavior (HMR, eval()-based React debugging, a dev "issues" overlay) instead of the production build. Caught via a stray "1 Issue" overlay button visible in a manual screenshot check; killed the stale process and re-ran the full suite with `CI=1` (forces a fresh `webServer`) to confirm all 12 e2e tests pass against a genuine production build.
- **Coverage**: 96.11% stmts/93.15% branches/96.66% funcs/96.08% lines (99 tests, up from 84) — all new `src/features/home/**` and `src/shared/ui/photo-frame/**` files are at 100%; the files below threshold in the report predate this PR (untouched, carried over from PR2a/PR2b).
- **Review budget**: this PR lands well above the 400-line budget — **997 insertions / 7 deletions (1004 total changed lines)**, `package-lock.json` unchanged (no new dependency). Atomic-unit justification: hero + stack strip + about + skills bento share one message-catalog namespace (`home.*`), one design-token extension, and the same boundaries-config fix required to ship any of them together — splitting further (e.g. hero-only vs. about-only) would fragment a single conceptually atomic "home sections part 1" deliverable without producing an independently reviewable slice. Commits are still organized as one work unit per section (10 commits, see PR body) so a reviewer can review incrementally within the PR.

### PR4 apply findings (2026-07-06)

- **Syntax highlighting decision (unspecified in design.md, resolved here)**: `rehype-pretty-code` + Shiki, single fixed `one-dark-pro` theme applied regardless of the site's light/dark toggle. This matches `design-reference/`'s own article page, whose code blocks are hardcoded dark (`#1a1a1f` background, `atom-one-dark` highlight.js theme) in every screenshot independent of the surrounding theme — so a per-theme dual-token Shiki setup would solve a problem the reference itself doesn't have. MDX compilation: `next-mdx-remote/rsc` (`MDXRemote`), matching the App Router/RSC-first approach design.md already committed to for i18n and motion.
- **`MDXRemote`/async-component RSC testing gap (jsdom limitation, same category as PR3a's anchor-hash finding)**: `<MDXRemote source={...} />` used as JSX is an async Server Component; `react-dom`'s synchronous client renderer (what Vitest/Testing Library actually runs) cannot resolve a *nested* async component and throws `"<MDXRemote> is an async Client Component"`. Worked around by calling `MDXRemote({...})` directly as a function and `await`-ing it inside `ArticleBody` (an async component itself) before returning JSX — the returned tree is then fully resolved by the time React ever sees it, so `render()` works normally. `ArticlePage` applies the same pattern for its own `ArticleBody` call. This is a *unit-test-only* workaround; the real Next.js RSC runtime (verified via `next build` + Playwright) resolves nested async components natively either way.
- **ESLint boundaries gaps found and fixed (extends PR3a's `ui -> ui` fix to `application`/`infrastructure`/`app`)**: same root cause as PR3a — `application` and `infrastructure` had no same-feature-captured allow entry for themselves, so a use-case's co-located `*.test.ts` importing the module it tests (e.g. `get-article.test.ts` importing `get-article.ts`) was rejected as "application may not import application". Fixed with the same `{ to: { type X, captured: { feature: "{{from.captured.feature}}" } } }` pattern already used for `ui`. Separately, `app` had no `domain` entry at all — `app/[locale]/blog/[slug]/page.tsx` (composition root) needs the `ArticleLocale` domain type to correctly type the repository/use-case wiring it performs; added `{ to: { type: "domain" } }` to `app`'s allow-list, since composition roots legitimately need domain types even though they don't import from `domain` for behavior.
- **Domain must not import `shared` — `ArticleLocale` deliberately duplicated**: `features/blog/domain/article-repository.ts` defines its own `type ArticleLocale = "es" | "en"` rather than importing `Locale` from `@/shared/i18n/routing`, per design.md's Boundary Rules (`domain` may import nothing). The two types are structurally identical, so real `Locale` values satisfy `ArticleLocale` with zero conversion at call sites.
- **4th, es-only fixture article (`content/blog/notas-breves/`)**: the "3 real bilingual sample articles" requirement covers the main portfolio content (`clean-architecture-nextjs`, `portfolio-tech-stack`, `design-patterns-daily`, all es+en). blog: Missing-Translation Fallback and its e2e/locale-switcher-awareness scenarios need a real article that only exists in one locale (not achievable with 3 articles that are all required to be bilingual) — `notas-breves` is a short, real, meaningful es-only post (about writing in Spanish first) added specifically to exercise that scenario end-to-end. It also happens to be the newest article by `date`, so it doubles as the "next article" wraparound case (`getNextArticle`'s newest-wraps-to-oldest path).
- **Categories are a fixed 3-id catalog**: `architecture` / `patterns` / `technologies` (`shared/content/categories.ts`), one sample article per category. Display labels live in `blog.categories.*` in both locale message catalogs; a dedicated test (`shared/content/category-catalog.test.ts`) keeps the catalog and the id list in exact 1:1 sync, and `mdx-loader.ts`'s `validateContentTree()` fails the build if any article's `category` isn't in this list (blog: Category id with no catalog label fails the build).
- **Tags are stable identifiers, not translated text (real bug caught by the loader's own cross-locale check)**: initially wrote Spanish-translated tag values in the `es.mdx` siblings (e.g. `"arquitectura"` alongside the `en.mdx` sibling's `"architecture"`). `real-content-tree.test.ts` (which runs `validateContentTree()` against the actual shipped `content/blog/` tree, not just synthetic fixtures) caught this immediately as a cross-locale divergence — tags, like category, MUST be the same stable identifier in every locale sibling. Fixed by using the English stable id in both locale files.
- **"Next article" card scoped as a small self-contained use case, not the full articles list**: `getNextArticle` (application layer) orders resolved articles chronologically (oldest → newest, wraparound after the newest) and returns a minimal teaser — deliberately independent of the full articles-list/filter feature (home-page: Embedded Article List, PR3b), which needs richer listing/pagination concerns this task doesn't require.
- **`Link` from `@/shared/i18n/navigation` accepts an explicit `locale` override**: used by `NextArticleCard` (the next article may only exist in the other locale) and implicitly relied upon for locale switcher article-awareness (task 4.6) — `LocaleSwitcher`'s existing generic `router.replace(pathname, { locale })` already preserves the full literal pathname including the slug segment, so switching locale on an article page naturally lands on the same slug in the target locale with zero new code; the article route's own fallback-aware rendering (task 4.3) then takes over. Verified end-to-end in `e2e/blog.spec.ts`, not by changing `LocaleSwitcher` itself.
- **Coverage**: 96.89% stmts / 91.85% branches / 97.22% funcs / 96.86% lines (176 tests, up from 99). 23/23 e2e passing (12 prior + 11 new in `e2e/blog.spec.ts`).
- **Review budget**: **2850 insertions / 6 deletions (2856 hand-written changed lines)** excluding `package-lock.json` (2321 generated lines from `gray-matter`/`next-mdx-remote`/`rehype-pretty-code`/`shiki`). Well above the 400-line budget — disclosed honestly. Atomic-unit justification: the MDX content pipeline (loader + schema + validation), the `blog` feature's full clean-architecture slice (domain/application/infrastructure/ui), the article route with its SEO metadata, and three real bilingual sample articles are one deliverable — a real MDX article page cannot be reviewed or verified end-to-end with any one of those pieces missing. This is consistent with the precedent set by PR2a/PR2b/PR3a (each also landed above budget with an atomic-unit justification under the pre-agreed `feature-branch-chain` strategy).

### PR3b apply findings (2026-07-06)

- **Home/blog boundary honored via `shared/content` + a dedicated home domain type**: `home` never imports the `blog` feature (tasks.md 3b.2's hard requirement). `src/shared/content/list-published-articles.ts` is a new shared use-case-style function (locale-filtered, latest-first, built directly on the PR4 `MdxLoader`) that the `app/[locale]/page.tsx` composition root calls and maps into `home`'s own zero-import `HomeArticleSummary` domain type (`src/features/home/domain/article-summary.ts`). `ArticlesSection` (home/ui) only ever sees that plain shape — no dependency on `blog`'s `Article` entity, its MDX repository, or its application layer.
- **Category filter labels reuse `blog.categories.*` deliberately, not a duplicate catalog**: category ids are a single stable, cross-feature catalog (`shared/content/categories.ts`); re-reading an existing i18n message key from `home/ui` is a data reference, not a code import, so it does not violate the ESLint boundaries rule (verified: `npm run lint` passes with the existing `ui -> shared` allow-list, no new boundary exceptions needed).
- **Filter pill set derivation**: pills are `ARTICLE_CATEGORY_IDS` filtered down to categories actually present in the current locale's articles (article-filter: Category Filtering — "union of `category` values across published articles ... a category with zero published articles ... has no pill"), in canonical catalog order rather than first-seen order, for deterministic pill ordering across renders/locales.
- **Real bug found and fixed by the mobile responsive e2e test (task 3b.4)**: adding three more `SectionNav` items (projects/articles/contact, six total) broke the existing 375px header layout — the nav no longer fit next to the locale switcher and theme toggle, causing real horizontal page overflow (not just a nav-specific issue; `document.documentElement.scrollWidth > innerWidth` failed for the *entire* page, including the pre-existing PR3a mobile test). Root-caused by writing a small Playwright script that flags every element whose `getBoundingClientRect()` exceeds the viewport — the culprit was the `<nav>` itself, not any section content. Fixed by collapsing the full anchor nav below Tailwind's `lg` breakpoint (`hidden lg:block`) rather than attempting a full mobile burger menu (present in `design-reference/` but out of scope for this task) — anchors remain reachable via scrolling and the footer's nav links on mobile. Documented as a deliberate deviation from the visual reference, not an oversight.
- **Projects section placeholder policy**: `PROJECTS[0]` ("Valencia Fullstack — this portfolio") is a real, verifiable entry — its repo link is this actual repository (`github.com/andresvalencia72/valenciafullstack`); no demo link is set (site not yet deployed). `PROJECTS[1]`/`PROJECTS[2]` are explicit placeholders: marked in code (`isPlaceholder: true` in `projects-data.ts`), in visible UI copy (a "Placeholder — replace with a real project" badge, plus problem/solution text that says so in both locales), and their demo/repo links point at the user's GitHub profile rather than a fabricated project repo — deliberately not reusing `design-reference/`'s fictional "Mercado Norte"/"Aula Viva" names, since presenting invented products as real work on a recruiter-facing portfolio would be dishonest.
- **Contact form field set follows the `contact` spec's validated shape, not `design-reference/`'s markup 1:1**: the reference's contact form includes a fourth "Asunto" (subject) field with no counterpart in the `contact` spec's Zod schema (`name`/`email`/`message`/`locale` only — see specs/contact/spec.md, Server-Side Input Validation). Shipping a subject field now would mean either building UI that PR6 immediately has to remove, or silently growing the validated shape beyond what's specced. Omitted by design; if a subject field is wanted, it should be added to the `contact` spec first.
- **Contact submit is genuinely inert, not fake-wired**: the `<button type="submit" disabled>` cannot fire a submit event at all; a code comment (`TODO(PR6, task 6.4)`) and a visible, translated helper line under the button (`home.contact.submitTodo`) both state the form is not connected yet, satisfying "disable submit or wire it to nothing with a clear TODO" from the PR3b brief.
- **Reused `blog.readingTime` catalog key for the articles list row**: rather than a second, near-duplicate "N min" key under `home.articles`, `ArticleRow` calls `useTranslations("blog")("readingTime", {minutes})` — same rationale as reusing `blog.categories.*` (single source of truth for a piece of copy shared across two features' UI, via i18n data, not a code import).
- **Coverage**: 97.33% stmts / 92.02% branches / 97.76% funcs / 97.29% lines (204 tests, up from 176). All new `src/features/home/ui/**`, `src/features/home/domain/**`, and `src/shared/content/list-published-articles.ts` files are at or near 100%. 27/27 e2e passing (23 prior + 4 new in `e2e/home-sections.spec.ts`: mobile projects/articles overflow, tablet, desktop, category-filter interaction).
- **Review budget**: **1427 insertions / 23 deletions (1450 hand-written changed lines)**, `package-lock.json` unchanged (no new dependency). Above the 400-line budget — disclosed honestly. Atomic-unit justification: the projects section, the articles list, and the functional category filter share one message-catalog namespace (`home.projects`/`home.articles`), one new shared `list-published-articles` use case, and the same `app/[locale]/page.tsx` composition-root rewrite (adding four sections + the article-list data fetch in a single coherent edit) — splitting further (e.g. projects-only vs. articles-only PRs) would fragment one conceptually atomic "home sections part 2" deliverable and require re-touching the same composition root and nav/footer files twice. Consistent with the precedent set by PR2a/PR2b/PR3a/PR4 (each landed above budget with an atomic-unit justification under the pre-agreed `feature-branch-chain` strategy).

### PR5a apply findings (2026-07-06)

- **`search_vector`: plain column, not `GENERATED ALWAYS AS ... STORED`** — design.md's per-locale regconfig requirement (`'spanish'` vs `'english'` depending on the row's `locale`) needs a `CASE` expression feeding `to_tsvector(regconfig, text)`. Postgres generated-column expressions MUST be `IMMUTABLE`; `to_tsvector(regconfig, text)` is classified `STABLE` in `pg_proc` (its output can change if the named text-search configuration is altered), so Postgres rejects it as a generation expression outright. This is consistent with design.md's own wording — `scripts/sync-search.ts` (PR8) "refreshes `search_vector`" via an explicit `UPDATE`, not a trigger or generated expression — so the schema declares `search_vector` as a nullable `tsvector` column (via Drizzle's `customType`, since `drizzle-orm` has no first-class `tsvector` builder) with a GIN index, populated exclusively by the PR8 sync script. No trigger was added; a generated-column or trigger-based alternative would need a hand-rolled `IMMUTABLE` wrapper function around `to_tsvector`, which is unnecessary complexity given the deterministic build-time sync script already owns writes.
- **Docker daemon unavailable in this sandbox** — `docker`/`docker compose` CLIs are present (client v29.2.1, Compose v5.1.0) but `docker info`/`docker compose up -d` fail with "Cannot connect to the Docker daemon" (Docker Desktop's backend isn't running and can't be started from this sandboxed shell). Verification approach: `drizzle-kit generate` is fully offline (parses `schema.ts` via static analysis, no DB connection needed) and produced `drizzle/0000_persistence_schema.sql` cleanly on the first attempt — table DDL, both enums, both unique indexes, the composite primary keys, and the GIN index all match design.md exactly (verified by reading the generated SQL). Applying the migration against a live Postgres instance is deferred to CI's real service container (`.github/workflows/ci.yml` `e2e` job), which now runs `npm run db:migrate` (`drizzle-kit migrate`) as a dedicated "Run database migrations" step before Playwright — this is the actual first real-Postgres verification point for this migration, per the task brief's guidance.
- **Migration file**: `drizzle/0000_persistence_schema.sql` (tag `0000_persistence_schema`), `drizzle/meta/0000_snapshot.json`, `drizzle/meta/_journal.json` — all committed. 5 tables, 2 enums (`locale`: es/en; `reaction_kind`: thumbs_up/heart/fire), 2 unique indexes (permanent dedupe), 1 GIN index, 2 composite primary keys (`article_search` PK(slug, locale); `rate_limits` PK(endpoint, key, window_start)).
- **`ip_hash` on `contact_messages` — purpose clarified**: HMAC-SHA256(requester IP, keyed by `VISITOR_HASH_SECRET`), retained per message specifically for abuse investigation (correlating a spam wave back to one hashed origin across multiple contact submissions after the fact) — distinct from `rate_limits`, which enforces the request-time throttle but doesn't retain history per message. Documented inline in `schema.ts`.
- **`shared/db/client.ts` wiring verified, no code change needed (task 5a.3)**: `getDb()` still returns a plain `drizzle-orm/postgres-js` instance (`drizzle(client)`, no `{ schema }` binding) — sufficient for the core query builder API (`db.select()/.insert()/.update()`) that PR5b's repository implementations and PR6/7/8's route handlers will use. `npm run typecheck` passes with `schema.ts` now present alongside `client.ts` (no circular import — `client.ts` still never imports `schema.ts`, and vice versa; repositories in PR5b will import both directly). The `rate_limits` atomic upsert pattern is documented at the point of use in `schema.ts`'s `rateLimits` table comment: `INSERT ... ON CONFLICT (endpoint, key, window_start) DO UPDATE SET count = count + 1 RETURNING count`, scoped per endpoint (each endpoint's rate limit is independent — see `security` capability for concrete per-endpoint request budgets).
- **CI seed step split into two named steps**: PR1's single placeholder "Seed database (migrations + search sync)" step is now split into "Run database migrations" (`npm run db:migrate`, wired this PR) and "Sync search index" (still a no-op placeholder, owned by task 8.1) — keeps each feature's CI ownership boundary explicit and matches the brief's instruction to wire only the migration half now.
- **Verification**: `npm run typecheck`, `npm run lint`, `npm run test:coverage` (204/204 tests, 97.33%/92.02%/97.76%/97.29% — unchanged from PR3b, `schema.ts` correctly excluded from the coverage `include` scope alongside `client.ts`), `npm run build` (13 routes, unaffected — confirms `schema.ts` introduces no build-time `DATABASE_URL` requirement), and a full local Playwright run on an alternate port (`PORT=3100`) — 27/27 e2e green, unchanged from PR3b (no PR5a feature surface consumes the DB yet; that starts at PR5b/PR6).
- **Review budget**: **~180 hand-written changed lines** (schema.ts ~140 new lines, `drizzle/0000_persistence_schema.sql` + `drizzle/meta/*` are drizzle-kit-generated, not hand-written, `.github/workflows/ci.yml` ~6 changed lines, tasks.md/state.yaml bookkeeping) — well under the 400-line budget, the first PR in the chain to land comfortably inside it. Atomic-unit justification: schema + its generated migration + the CI migration-wiring are one inseparable deliverable per the `persistence` spec's "a schema change MUST ship with a migration in the same change" requirement; repository interfaces/implementations are correctly deferred to PR5b (not built here, per the task brief).

### PR5b apply findings (2026-07-07)

- **Scope decision — which repositories PR5b actually builds**: tasks 5b.1/5b.2 read generically ("repo interfaces per feature domain" / "Drizzle repo impls per feature infrastructure"), but three of the five persistence tables already have their concrete build-out explicitly assigned to a later phase in their own task text: `article_search`'s upsert/prune is task 8.1's "full reconcile" deliverable (shape fully dictated by the sync script, not yet designed), and `rate_limits`'s atomic upsert is explicitly named in task 6.3 ("atomic upsert on `rate_limits`"). Building **repository interfaces + Drizzle implementations for all four "generic CRUD/dedupe" tables that don't require any business-specific decision** cleanly separates "persistence capability" (this phase) from "business orchestration capability" (validation, honeypot, HTTP status codes, response shaping — PR6/PR7/PR8's application layers), while correctly deferring `article_search`'s repository to PR8 (task 8.1 owns its exact shape). Concretely, PR5b ships: `ContactMessageRepository` (contact), `ArticleViewRepository` + `ArticleReactionRepository` (engagement), and `RateLimitRepository` — the last one shared (see next finding), even though task 6.3's wording could be read as assigning it to PR6. Building it once now, ahead of PR6, avoids identical Drizzle upsert logic being written three times (contact, engagement, search each rate-limit independently per the `security` spec) and gives PR6/PR7/PR8 a ready-made, already-pglite-tested primitive to call from their application-layer orchestration.
- **`RateLimitRepository` placed under `shared/rate-limit/`, not a feature `domain/`**: the `rate_limits` table is cross-cutting — contact, engagement, and search each enforce their own independent per-endpoint limit against the identical schema shape (endpoint, key, window_start, count), per the `security` spec's "each endpoint maintains its own independent limit window" requirement. It has no single owning feature. `shared -> shared` and `infrastructure -> shared` are both already-allowed edges in `eslint.config.mjs`'s boundaries config, so `shared/rate-limit/rate-limit-repository.ts` (interface, zero drizzle) + `shared/rate-limit/drizzle-rate-limit-repository.ts` (impl) is structurally identical in spirit to the persistence spec's "Domain Repository Interfaces" requirement (interface has zero infra imports; impl implements it via Drizzle) — just scoped to `shared/` instead of one feature's `domain/`, mirroring `shared/db`'s existing role as home for cross-feature persistence primitives. Documented here as a deliberate, defensible divergence from "repo interfaces per feature domain"'s literal wording, not an oversight.
- **pglite escape hatch verified — full parity, no testcontainers fallback needed**: design.md's Testing Strategy flagged "during PR5b, verify that pglite supports the `spanish`/`english` regconfig and `websearch_to_tsquery` with parity to real Postgres; if parity fails, fall back to testcontainers." Verified via a throwaway script (migration applied via `drizzle-orm/pglite/migrator` against the real generated `drizzle/0000_persistence_schema.sql`, then `to_tsvector('spanish', ...)` + `websearch_to_tsquery('spanish', ...)` + `ts_rank` + the `article_search_search_vector_idx` GIN index all worked identically to real Postgres) before writing any repository code. No fallback triggered — pglite is confirmed as the integration-test database for all of PR5b (and, by extension, PR8's future search repository).
- **Repository constructors accept an injected `Database` handle, not `getDb()` internally**: every Drizzle implementation is a factory function (`createDrizzle*Repository(db)`), matching the existing `createMdxArticleRepository(loader)` pattern from `blog`/infrastructure (PR4) rather than a class calling `getDb()` at the point of use. This makes every repository trivially testable against a pglite `Database` in tests while the `app/` composition root (PR6/PR7/PR8) will call `createDrizzle*Repository(getDb())` for the production Postgres connection. A new shared type, `src/shared/db/database.ts` (`Database = PgDatabase<any, any, any>`, from `drizzle-orm/pg-core`), is the common parameter type both the production `postgres-js` driver and the pglite test driver structurally satisfy (both extend the same `PgDatabase` class) — declarative type-alias only, excluded from coverage on the same basis as `schema.ts`/`client.ts`.
- **`src/shared/db/create-pglite-test-db.ts`**: a test-only bootstrap (also excluded from coverage, same basis) that spins up a fresh in-memory pglite instance and applies the real generated migration file — not a hand-maintained schema copy — so every repository integration test runs against the exact DDL that ships to production. Reused by all four pglite test suites in this PR.
- **Domain interface files ship without a dedicated test file (documented TDD exception)**: `contact-message-repository.ts`, `article-view-repository.ts`, `article-reaction-repository.ts`, and `rate-limit-repository.ts` are pure type/interface exports with zero runtime behavior — "purely structural, one possible output, no branching," the explicit skip-triangulation condition in `strict-tdd.md`. The persistence spec's "Domain layer has no infra imports" scenario (zero references to `drizzle-orm`) is enforced structurally and more strictly than a source-grep test could verify: `eslint.config.mjs`'s `domain: { allow: [] }` rule denies these files literally ANY import, not just `drizzle-orm` specifically — re-verified via `npm run lint` after writing each interface. See the TDD Cycle Evidence table below for the explicit "Triangulation skipped" annotation per file.
- **`ArticleViewRepository.countViews` — a real bug caught by triangulation, not a smoke test**: the first draft wrote `rows[0]?.value ?? 0` defensively. Coverage flagged the `?? 0` branch as never hit (BRF:2/BRH:1 in the v8 lcov report). Root cause: `count()` with no `GROUP BY` always returns exactly one row from Postgres (0 when nothing matches, per SQL aggregate semantics — this differs from the reaction repository's `GROUP BY kind` query, where an entirely-absent kind legitimately produces zero rows and the manual zero-fill is real, exercised logic). Fixed by removing the unreachable fallback (`const [row] = await db.select(...); return row.value;`) and adding an explicit triangulation test for the zero-views case (a slug that was never viewed) to prove the "0 rows still returns one row with value 0" path is real behavior, not untested from either side.
- **Verification**: `npm run typecheck`, `npm run lint` (repo-wide, 0 errors), `npm run test:coverage` — 215/215 tests (57 test files, up from 204/53), 97.46%/92.02%/97.93%/97.43% (up from PR5a's 97.33/92.02/97.76/97.29 — coverage did not regress; all four new repository implementation files are individually 100% lines / 0 uncovered branches per the raw lcov report, `Database`/`create-pglite-test-db.ts` correctly excluded), `npm run build` (still 13 routes, unaffected — no `app/` route consumes these repositories yet, that starts at PR6/PR7). Docker unavailable in this sandbox as in PR5a; pglite requires no Docker (in-process WASM Postgres), so all four repository integration tests ran natively without any workaround. Playwright e2e was not re-run — no `app/` surface changed, and the existing 27/27 e2e suite carries over unaffected from PR5a (per-PR full e2e reverification resumes once PR6/PR7 add real routes exercising these repositories).
- **Review budget**: **628 hand-written changed lines (623 insertions / 5 deletions)**, `package-lock.json` unchanged in this count (one new dependency, `@electric-sql/pglite`, generated lockfile lines disclosed separately) — measured via `git diff --stat feat/pr5a-persistence-schema...feat/pr5b-persistence-repos`. Above the 400-line budget, disclosed honestly per `feature-branch-chain`. Breakdown: 4 domain interfaces (~107 lines), 4 Drizzle implementations (~163 lines), 4 pglite test files (~260 lines), `database.ts` + `create-pglite-test-db.ts` (~51 lines), `vitest.config.ts` (+7), tasks.md/state.yaml bookkeeping (+39/-5). Atomic-unit justification: the four repositories share one new test-infrastructure primitive (`createPgliteTestDb`) and one new shared type (`Database`) that only make sense introduced together with their first real consumers; splitting further (e.g. contact in one PR, engagement in another) would mean re-justifying and re-reviewing the same pglite bootstrap twice for no independent deliverable value — each individual repository is a small, fully-tested unit already organized as its own commit (see PR body) so a reviewer can review incrementally within the PR. Consistent with the precedent set by PR2a/PR2b/PR3a/PR4/PR3b (each landed above budget with an atomic-unit justification under the pre-agreed `feature-branch-chain` strategy); PR5a remains the sole PR in the chain that landed comfortably under budget.

#### TDD Cycle Evidence (PR5b)

| Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
|------|-----------|-------|------------|-----|-------|-------------|----------|
| 5b.1 | N/A (interfaces only) | N/A | N/A (new) | N/A | N/A | Triangulation skipped: purely structural interface/type exports, zero runtime behavior, zero-import enforced by eslint boundaries `domain: allow: []` | N/A |
| 5b.2 (contact) | `drizzle-contact-message-repository.test.ts` | Integration (pglite) | N/A (new) | ✅ Written (referenced non-existent impl) | ✅ Passed | ✅ 2 cases (single save + multiple independent rows) | ➖ None needed |
| 5b.2 (engagement views) | `drizzle-article-view-repository.test.ts` | Integration (pglite) | N/A (new) | ✅ Written | ✅ Passed | ✅ 4 cases (first view, repeat-view dedupe, zero-views, multi-visitor scoping) | ✅ Removed unreachable `?? 0` branch after coverage flagged it (see findings) |
| 5b.2 (engagement reactions) | `drizzle-article-reaction-repository.test.ts` | Integration (pglite) | N/A (new) | ✅ Written | ✅ Passed | ✅ 3 cases (first reaction, duplicate dedupe, per-kind independence) | ➖ None needed |
| 5b.2 (rate limit) | `drizzle-rate-limit-repository.test.ts` | Integration (pglite) | N/A (new) | ✅ Written | ✅ Passed | ✅ 3 cases (first request, atomic increment, per-endpoint scoping) | ➖ None needed |

### Test Summary (PR5b)
- **Total tests written**: 12 (2 contact + 4 engagement-views + 3 engagement-reactions + 3 rate-limit)
- **Total tests passing**: 215/215 (full suite, up from 204/204 at PR5a)
- **Layers used**: Integration (pglite) 4 files/12 tests; no new Unit/E2E tests this phase
- **Approval tests** (refactoring): None — no refactoring tasks
- **Pure functions created**: 0 (all four are stateful repository factories over a DB handle, by nature — see Pure Function Preference note in strict-tdd.md: "don't force it where it doesn't fit")

### CSP dev-mode hotfix findings (2026-07-07, post-PR5b)

- **Out-of-plan hotfix on its own branch/PR**: `fix/csp-dev-unsafe-eval` (PR #10, base=`feat/pr5b-persistence-repos`, new chain tip). React's development runtime requires `eval()`, so the static ADR-0007 CSP shipped in task 1.8 broke `next dev` with console errors. Fixed by extracting a `buildSecurityHeaders(isDev)` builder in `src/shared/config/security-headers.ts`: `'unsafe-eval'` is appended to `script-src` only when `isDev` is true; the production header set remains byte-identical to ADR-0007. ADR-0007 (`docs/adr/0007-csp-script-src-strategy.md`) updated to document the dev-only exception.
- **TDD**: test-first against `security-headers.test.ts` — dev build must include `'unsafe-eval'` in `script-src`, production build must not (asserted equal to the ADR-0007 header set). Full suite went 215/215 → **221/221** (coverage 97.48%/92.16%/97.95%/97.44%, no regression). Additionally verified at runtime via `curl` header inspection against a live `next dev` server.
- **Relation to task 11.1**: no scope change — 11.1 still verifies the production header set, which this hotfix leaves unchanged.

### PR6 apply findings (2026-07-07)

- **Docker was actually available this session** — unlike PR5a/PR5b/the CSP hotfix (all of which recorded "Docker unavailable in this sandbox"), `docker info`/`docker compose up -d` succeeded here, so `compose.yaml`'s real Postgres 16 container ran locally. This is the first PR verified end-to-end against a genuine running Postgres instance rather than pglite alone: `npm run db:migrate` applied the PR5a/PR5b migration cleanly, `npm run build` produced a real production build, and the full Playwright e2e suite (32/32, up from 27/27) ran against `next start` + that container, including every new contact/privacy test. Recorded here because it changes what "verified" means for this PR relative to its predecessors — treat this as a one-off environment capability, not a new baseline assumption for future PRs.
- **`application -> shared` eslint-boundaries edge added, not worked around**: `submitContactMessage` (application) needs the `RateLimitRepository` type and `checkRateLimit` from `shared/rate-limit/*` — PR5b's own findings explicitly anticipated this ("gives PR6/PR7/PR8 a ready-made ... primitive to call from their application-layer orchestration"), but the boundaries config's `application` rule had no `shared` entry (only `infrastructure`/`ui`/`app` did). Added `{ to: { type: "shared" } }` to the `application` allow-list in `eslint.config.mjs`, documented inline, rather than duplicating `RateLimitRepository`'s shape into every feature's domain — same "fix the boundary when a real need surfaces" precedent as PR3a's `ui -> ui` fix and PR4's `application -> application`/`app -> domain` fixes.
- **`domain` literally cannot import ANYTHING, not even a sibling domain file in the same feature** — re-confirmed while writing `contact/domain/contact-email-sender.ts`: `eslint.config.mjs`'s `domain: { allow: [] }` blocks same-feature `domain -> domain` too (there is no capture-scoped self-entry for `domain`, unlike `application`/`infrastructure`/`ui`). `ContactLocale` is therefore duplicated verbatim in `contact-email-sender.ts` rather than imported from `contact-message-repository.ts` — same rationale as `blog`'s `ArticleLocale` duplication (PR4).
- **Check-order orchestration lives in `submitContactMessage` (application), not the route handler**, per the contact spec's explicit instruction — verified structurally: `app/api/contact/route.ts` contains zero conditionals beyond parsing the body and forwarding the mapped response; every branch (rate limit → honeypot → validation → persist → email) is in `src/features/contact/application/submit-contact-message.ts`, fully covered by the `src/**` gate.
- **Honeypot check is intentionally lenient, not a second schema** — `isHoneypotTriggered` reads the raw, unvalidated body directly (no Zod parse) because it MUST run before Zod validation per the check-order requirement; a malformed payload (wrong types, missing fields) must still be able to trip the honeypot. Field name `company`, positioned off-screen (`absolute left-[-9999px]`) rather than `display:none` — some bots skip `display:none` fields specifically because it's the most common honeypot tell.
- **`RESEND_API_KEY` absent locally (as flagged) and `CONTACT_EMAIL_TO`/`CONTACT_EMAIL_FROM` are new optional env vars, not in the original design.md env matrix** — added because the contact spec requires an actual destination address, which no existing env var covered. `CONTACT_EMAIL_TO` has no default (absent → `createContactEmailSender()` returns the always-rejecting "unconfigured" sender, degrading to the same HTTP 202 path as a missing `RESEND_API_KEY`, per contact spec's "Email service not configured" scenario). `CONTACT_EMAIL_FROM` defaults to Resend's sandbox sender (`onboarding@resend.dev`), which sends without a verified domain — documented in README and inline in `env.ts`.
- **Email sender factory pattern mirrors the repository factory precedent**: `createContactEmailSender()` (infrastructure, reads `getEnv()`) picks between `createFakeContactEmailSender()` (EMAIL_DRIVER=fake, always resolves — CI's deterministic e2e path), `createUnconfiguredContactEmailSender()` (always rejects — missing key/destination), and `createResendContactEmailSender({ client, to, from })` (real Resend SDK, injected client for testability — same `createDrizzle*Repository(db)` injection pattern from PR5b). `submitContactMessage`'s single try/catch around `emailSender.send()` treats all three "not truly sent" outcomes identically as "delayed" (202), which is exactly the point: the use-case has zero knowledge of *why* email failed.
- **Cross-feature UI composition stays at the `app/` level, not via a `home -> contact` import**: `ContactSection` (home/ui) was changed from rendering its own `<form>` inline (PR3b's static shell) to accepting the form as `children` — `app/[locale]/page.tsx` (composition root) now renders `<ContactSection><ContactForm /></ContactSection>`, where `ContactForm` is `contact/ui`'s own component. This follows the same rule task 3b.2 established for `home` never importing `blog` directly: `ui -> ui` cross-feature imports are eslint-boundaries-disallowed (only same-feature `ui -> ui` is allowed), so the only legal place to wire two features' UI together is the `app/` composition root, which allows importing any `ui`.
- **Client IP resolution has a hard platform limitation, not fully solved**: `resolveClientIp()` trusts `x-forwarded-for`'s first entry (correct in production behind Vercel's sanitizing proxy) but falls back to a fixed `127.0.0.1` constant when the header is absent, rather than a real socket address — App Router Route Handlers on the Node.js runtime expose no direct socket-address API. This means every local/CI request without an XFF header shares one `ip_hash` bucket; harmless for correctness (rate limiting still works, just coarse-grained locally) and exactly why the e2e suite truncates `rate_limits` for the `contact` endpoint in a `beforeAll` hook rather than relying on IP diversity.
- **Privacy page email address is a hardcoded placeholder, not env-derived, by necessity**: `/[locale]/privacy` is a fully static route (`generateStaticParams` inherited from the locale layout, no page-level dynamic API), so its component body runs during `next build`'s static generation — calling `getEnv()` there would fail the build (`DATABASE_URL`/`VISITOR_HASH_SECRET` fail-fast, which `next build` MUST NOT require per design.md). `PRIVACY_CONTACT_EMAIL = "privacy@valenciafullstack.dev"` is therefore a literal constant in `app/[locale]/privacy/page.tsx`, documented inline as needing a real, monitored inbox before production launch — same category of documented limitation as PR1's `.env.example` sandbox-write restriction.
- **Contact e2e rate-limit isolation**: `e2e/contact.spec.ts`'s `beforeAll` truncates `rate_limits WHERE endpoint = 'contact'` via a direct `postgres` client against `DATABASE_URL` before the suite runs (skips gracefully if `DATABASE_URL` is unset), per the Implementation Notes' guidance — verified locally against the real Docker Postgres container across repeated runs with zero 429s.
- **Second e2e negative case exercises the real server round-trip, not a client-side HTML constraint**: an initial draft asserted the browser's native `type="email"` validity instead of a real request, which proves nothing about the server (the client's `maxLength`/`type="email"` constraints mirror the server schema by design, so a real invalid submission can't be produced through the rendered form at all). Replaced with `page.evaluate(() => fetch("/api/contact", ...))` calls that bypass the DOM entirely for both the honeypot-triggered and malformed-payload cases — genuine server verification against the real running route.

#### TDD Cycle Evidence (PR6)

| Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
|------|-----------|-------|------------|-----|-------|-------------|----------|
| 6.3 | `shared/security/hmac-sha256.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 2 cases (deterministic digest + different-secret divergence) | ➖ None needed |
| 6.3 | `shared/security/resolve-client-ip.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 3 cases (XFF present, absent, blank-first-entry) | ➖ None needed |
| 6.3 | `shared/rate-limit/check-rate-limit.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 3 cases (under limit, over limit, window-boundary math) | ➖ None needed |
| 6.2 | `shared/config/env.test.ts` | Unit | ✅ 7/7 | ✅ Written | ✅ Passed | ✅ 2 new cases (CONTACT_EMAIL_FROM default, CONTACT_EMAIL_TO no-default) | ➖ None needed |
| 6.1 | `features/contact/application/contact-submission-schema.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 11 cases (valid + 6 field-limit violations + 4 honeypot cases) | ➖ None needed |
| 6.1/6.2/6.3 | `features/contact/application/submit-contact-message.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 7 cases (sent, rate-limited, honeypot, invalid, persistence-failed, delayed, rate-limit-key assertion) | ➖ None needed |
| 6.1-6.3 | `features/contact/application/map-submit-contact-result.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 6 cases (one per result kind) | ➖ None needed |
| 6.2 | `features/contact/infrastructure/fake-contact-email-sender.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 2 cases | ➖ None needed |
| 6.2 | `features/contact/infrastructure/unconfigured-contact-email-sender.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 2 cases | ➖ None needed |
| 6.2 | `features/contact/infrastructure/resend-contact-email-sender.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 2 cases (success, API error) | ➖ None needed |
| 6.2 | `features/contact/infrastructure/create-contact-email-sender.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 4 cases (fake, missing key, missing destination, fully configured) | ➖ None needed |
| domain (6.2) | `contact-email-sender.ts` | N/A | N/A (new) | N/A | N/A | Triangulation skipped: purely structural interface/type exports, zero runtime behavior, zero-import enforced by eslint boundaries `domain: allow: []` | N/A |
| 6.4 | `features/contact/ui/contact-form.test.tsx` | Unit (component) | N/A (new) | ✅ Written | ✅ Passed | ✅ 7 cases (render, honeypot shape, success, 400, 429, network failure, pending-disabled) | ✅ Extracted `updateField` helper to avoid 4 near-identical inline handlers |
| 6.4 | `features/home/ui/contact-section.test.tsx` | Unit (component) | ✅ 4/4 | ✅ Written (children-composition behavior) | ✅ Passed | ➖ Single (structural prop change) | ✅ Clean — removed 45 lines of now-dead static markup |
| 6.6 | `features/home/ui/site-footer.test.tsx` | Unit (component) | ✅ 3/3 | ✅ Written | ✅ Passed | ➖ Single (one new link) | ➖ None needed |
| 6.5 | `e2e/contact.spec.ts` | E2E | N/A (new) | ✅ Written | ✅ Passed (against real Docker Postgres) | ✅ 3 cases (valid submission, honeypot, malformed 400) | ➖ None needed |
| 6.6 | `e2e/privacy.spec.ts` | E2E | N/A (new) | ✅ Written | ✅ Passed | ✅ 2 cases (en via footer link, es direct) | ➖ None needed |

#### Test Summary (PR6)
- **Total tests written**: 51 (2 hmac + 3 resolve-client-ip + 3 check-rate-limit + 2 env + 11 contact-submission-schema + 7 submit-contact-message + 6 map-submit-contact-result + 2 fake-sender + 2 unconfigured-sender + 2 resend-sender + 4 create-contact-email-sender + 7 contact-form + 1 contact-section (net: 3 kept, 1 removed, 1 added) + 1 site-footer (net: +1) + 3 e2e/contact.spec.ts + 2 e2e/privacy.spec.ts)
- **Total tests passing**: 272/272 unit+integration (up from 221/221), 32/32 e2e (up from 27/27, verified against a real local Docker Postgres container)
- **Layers used**: Unit 49, Component (Testing Library) included in the 49, E2E 5
- **Approval tests** (refactoring): None — no behavior-preserving refactor of pre-existing logic; `contact-section.tsx`/`site-footer.tsx` changes are additive (children prop, new link)
- **Pure functions created**: `hmacSha256Hex`, `resolveClientIp`, `checkRateLimit`, `isHoneypotTriggered`, `mapSubmitContactResultToResponse` — 5; `submitContactMessage` and the three email-sender factories are intentionally impure (I/O orchestration), per strict-tdd.md's "don't force it where it doesn't fit"

#### Verification (PR6)
- `npm run typecheck` — clean.
- `npm run lint` — clean (0 errors after the `application -> shared` boundaries addition).
- `npm run test:coverage` — **272/272 tests passing** (up from 221/221), coverage **97.88% / 93.77% / 98.18% / 97.85%** (stmts/branches/funcs/lines — up from 97.48/92.16/97.95/97.44, no regression; every new file is 100% lines with 0 uncovered branches per the raw report, `contact-email-sender.ts` domain interface correctly has zero eligible statements).
- `npm run build` — 16 routes (up from 13): `/[locale]` (es/en), `/[locale]/blog/[slug]` (8 paths), `/[locale]/privacy` (es/en, both statically prerendered — confirms `getEnv()` is never called at build time), `/api/contact` (dynamic, as expected for a POST-only route handler).
- `npm run test:e2e` — **32/32 passing** (up from 27/27), run against a real local Docker Postgres container (`compose.yaml`, migrated via `npm run db:migrate`) and a genuine `next build` + `next start` production server — the first PR in this chain able to verify its DB-backed routes against real Postgres locally rather than deferring entirely to CI.

#### Review budget (PR6)
- **1923 hand-written changed lines (1833 insertions / 90 deletions)** — measured via `git diff --stat fix/csp-dev-unsafe-eval...feat/pr6-contact -- . ':!package-lock.json'` (excludes `package-lock.json`'s generated lines for the new `resend` dependency). Well above the 400-line budget, disclosed honestly per `feature-branch-chain` — following PR5b's own lesson ("always verify line-count claims against actual `git diff --stat`, don't estimate from memory"), this number is the real measured diff, not an estimate. Breakdown: contact `application` layer (schema/use-case/response-mapper) + tests (~500 lines), contact `infrastructure` (4 email senders + factory) + tests (~350 lines), 1 domain port (~30 lines), `ContactForm` + test (~320 lines), shared `security`/`rate-limit` primitives + tests (~190 lines), privacy page + i18n message catalogs (~140 lines), e2e specs (~120 lines), `ContactSection`/`site-footer`/`page.tsx`/route handler/`env.ts` diffs (~140 lines, net negative on `contact-section.tsx` — PR3b's static markup was deleted), eslint.config.mjs boundary addition (~9 lines), tasks.md/state.yaml/README bookkeeping (~85 lines). Atomic-unit justification: a contact form that "works" end to end requires validation, persistence, email delivery with its three-way degradation, rate limiting, spam mitigation, the UI wiring, and the privacy disclosure the contact spec itself mandates (task 6.6 is listed under the same phase specifically because it documents what the form's own persistence does) — splitting further (e.g. API-only vs. UI-only) would ship an unreviewable half of one deliverable, same reasoning as every prior above-budget PR in this chain (PR2a/PR2b/PR3a/PR4/PR3b/PR5b).

### PR7 apply findings (2026-07-07)

- **No new env var actually needed — `VISITOR_HASH_SECRET` was already in the schema.** Tasks.md/the apply brief both call out adding `VISITOR_HASH_SECRET` to `shared/config/env.ts`, but PR1's `env.ts` (task 1.4) already added it as a required fail-fast variable ("fail-fast only on missing `DATABASE_URL` and `VISITOR_HASH_SECRET`") — anticipating this PR before it existed. Verified by reading `env.ts` before writing any code; zero env schema changes shipped in PR7.
- **No eslint-boundaries edges needed either — every dependency PR7 needed was already legal.** Unlike PR4 (`application -> application`, `infrastructure -> infrastructure`, `app -> domain`), PR3a (`ui -> ui` same-feature), and PR6 (`application -> shared`), this PR added zero new rows to `eslint.config.mjs`. `record-article-view`/`record-article-reaction`/`getArticleEngagementSummary` (application) need `shared/rate-limit/*` — already legal since PR6. `mdx-article-slug-checker.ts` (infrastructure) needs `shared/content/mdx-loader` — `infrastructure -> shared` was already legal since PR5b. `EngagementPanel` (ui) needs only its own feature's `domain` types — no new edge. The one genuinely new cross-feature need (validating a slug against blog's published article set without importing `blog`) was solved by depending on `shared/content` directly (already `shared`-typed, not `blog`-typed) rather than by widening a boundary rule.
- **`ArticleSlugChecker` domain port, not a direct `MdxLoader` injection into `application`**: `shared/content`'s `MdxLoader` is a structurally legal `application` dependency (any layer may import `shared/*`, per PR6's `RateLimitRepository` precedent), but injecting the full loader (multiple unrelated capabilities: `readArticleFile`, `validateContentTree`, filesystem I/O) would widen each use-case's dependency surface far beyond "is this slug published?". Added a narrow `ArticleSlugChecker` domain port instead, implemented in `engagement/infrastructure/mdx-article-slug-checker.ts` against the exact same `mdxLoader` singleton the PR4 article route's `generateStaticParams` uses — one source of truth for the published article set, zero cross-feature import of `blog` itself, and the use-cases only ever see a one-method interface (interface segregation).
- **`EngagementWriteResult`/`mapEngagementWriteResultToResponse` shared between `views` and `reactions`, not duplicated**: both endpoints apply the byte-identical rate-limit -> validate -> unknown-slug-reject -> persist orchestration and the byte-identical HTTP mapping (`recorded` -> 204 no body, `invalid` -> 400, `not-found` -> 404, `rate-limited` -> 429, `persistence-failed` -> 503), per the engagement spec's explicit requirement that first-write and duplicate-write "never distinguish the two cases." One shared result type + one shared mapper (in `record-article-view.ts`'s originating commit) avoids maintaining two copies of an identical switch statement.
- **Each write endpoint gets its own independent rate-limit `endpoint` string** (`engagement:views`, `engagement:reactions`, `engagement:summary`), all 3 backed by the same `rate_limits` table/`checkRateLimit` helper PR6 introduced — confirmed structurally by a dedicated assertion in each use-case's test suite (`incrementAndGet` called with the exact endpoint string), not just by convention, per the security spec's "exhausting one endpoint's budget MUST NOT affect another endpoint's counter."
- **`visitor_hash` value object placed in `shared/security`, not `engagement/domain`** (task 7.1's literal wording says "domain"): hashing requires `node:crypto`, and PR6 already established that `domain` layers stay import-free by pushing `hmacSha256Hex` itself into `shared/security` rather than `contact/domain`. The `security` spec independently lists "Hashed Visitor Keys, No Tracking Cookies" as its own cross-cutting requirement (not scoped to `engagement`), so `deriveVisitorHash` sits alongside `hmacSha256Hex`/`resolveClientIp` as a third shared security primitive — combines IP + user agent with a `\0` (NUL) separator (not a printable delimiter like a space) specifically because a `User-Agent` header can legitimately contain spaces, which would otherwise let two distinct (ip, userAgent) pairs concatenate to the same combined string.
- **Self-caught bug: a literal NUL byte in a committed source file, not just its string value.** The first draft of `derive-visitor-hash.ts` was written with an actual `\x00` byte in the separator constant rather than a `"\0"` text escape — functionally identical at runtime (all 4 unit tests passed immediately), but it made `git diff`/GitHub render the entire file as binary ("Bin 0 -> 1477 bytes"), which would have shipped an unreviewable file in the PR. Caught by inspecting `git diff --stat` before opening the PR (not by any test), fixed in a small follow-up commit that re-encodes the same value as a normal escape sequence — zero behavior change, confirmed by re-running the file's own test suite untouched.
- **`ArticlePage` gains a `children` slot, same pattern as PR6's `ContactSection`**: cross-feature `ui -> ui` imports are eslint-boundaries-disallowed (only same-feature `ui -> ui` is allowed), so `blog/ui`'s `ArticlePage` cannot import `engagement/ui`'s `EngagementPanel` directly. The `app/[locale]/blog/[slug]/page.tsx` composition root (which may import any feature's `ui`) renders `<ArticlePage ...><EngagementPanel slug={slug} /></ArticlePage>`, mirroring PR6's `<ContactSection><ContactForm /></ContactSection>` — confirms this is now an established, reusable composition pattern for the codebase, not a one-off.
- **`useEngagement` derives `isReady` as an aliased boolean (`status === "ready" && views !== null && reactions !== null`) instead of a `?? 0` defensive fallback in `EngagementPanel`**: `useEngagement` always sets `status`, `views`, and `reactions` together in the same React state-update batch, so whenever `status === "ready"`, `views`/`reactions` are guaranteed non-null — a real invariant, not an assumption. An initial draft used `views ?? 0` at the render call site "just in case"; coverage flagged that fallback as unreachable (same category as PR5b's `ArticleViewRepository.countViews` finding). Fixed by deriving `isReady` once and letting TypeScript's aliased-condition narrowing (TS 4.4+) eliminate the dead branch entirely, rather than writing a test for a state combination the hook can never actually produce.
- **Manual verification against real Postgres caught nothing wrong — confirmed the dedupe is visitor-scoped, not client-scoped**: after `docker compose up` + `npm run db:migrate` + `npm run build`, curl'd `POST /api/engagement/views` twice for the same slug from the same terminal session (identical IP fallback, identical `curl` User-Agent) and got exactly one new `article_views` row — the second call correctly deduped. A third row for the same slug came from Playwright's own headless-Chromium visit during the e2e run moments earlier (different User-Agent -> different `visitor_hash` -> a legitimately distinct visitor, not a bug); confirmed by inspecting `SELECT slug, visitor_hash, created_at FROM article_views` directly.
- **Stale dev server on port 3000 caused 3 spurious e2e failures on the first run — same root cause as PR3a's documented finding, now recurring.** An `npm run dev` process from an earlier, unrelated session (started before this PR7 apply session began) was still listening on port 3000; Playwright's `reuseExistingServer: !CI` attached to it instead of a fresh `next start` build, producing 3 failures in `e2e/contact.spec.ts` (a hung success-message wait, two "Unexpected end of JSON input" `page.evaluate` failures) that had nothing to do with PR7's own changes — verified independently via direct `curl` calls against a fresh `next start` on an alternate port, which returned correct JSON for every case. Killed the stale process and re-ran with `CI=1` (forces a fresh `webServer`, matching PR3a's fix) to get a clean 32/32. Recorded here as a second occurrence of the same class of environment hazard — future sessions should check `lsof -i :3000` (or run with `CI=1`) before trusting a red e2e run.
- **Coverage improved on every one of the four tracked metrics, not just "no regression"**: 98.30%/93.98%/98.43%/98.28% (stmts/branches/funcs/lines), up from PR6's 97.88%/93.77%/98.18%/97.85% — every new file in `features/engagement/**` and `shared/security/derive-visitor-hash.ts` is 100% lines, and the two hook branches coverage initially flagged (`readReactedFlag`'s catch path, the effect's `cancelled`-guard early return) were closed with dedicated triangulation tests (a throwing `localStorage.getItem`, an unmount-mid-fetch case) rather than skipped.

#### TDD Cycle Evidence (PR7)

| Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
|------|-----------|-------|------------|-----|-------|-------------|----------|
| 7.1 | `shared/security/derive-visitor-hash.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 3 cases (different IP, different UA, unambiguous separator) | ➖ None needed |
| 7.2/7.3/7.4 (slug validation) | `engagement/infrastructure/mdx-article-slug-checker.test.ts` | Unit (fake loader) | N/A (new) | ✅ Written | ✅ Passed | ✅ 1 case (absent slug) | ➖ None needed |
| domain (7.2/7.3/7.4) | `article-slug-checker.ts` | N/A | N/A (new) | N/A | N/A | Triangulation skipped: purely structural interface export, zero runtime behavior, zero-import enforced by eslint boundaries `domain: allow: []` | N/A |
| 7.2 | `application/view-request-schema.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 4 cases (empty slug, missing slug, non-string slug, null body) | ➖ None needed |
| 7.2 | `application/record-article-view.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 6 cases (repeat view, invalid, not-found, rate-limited, persistence-failed, rate-limit key assertion) | ➖ None needed |
| 7.2/7.3 | `application/map-engagement-write-result.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 5 cases (one per result kind + a no-PII-leakage sweep) | ➖ None needed |
| 7.3 | `application/reaction-request-schema.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 4 cases (invalid kind, missing kind, empty slug, null body) | ➖ None needed |
| 7.3 | `application/record-article-reaction.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 6 cases (duplicate reaction, invalid kind, not-found, rate-limited, persistence-failed, independent rate-limit key) | ➖ None needed |
| 7.4 | `application/get-article-engagement-summary.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 5 cases (not-found, rate-limited, view-count throw, reaction-count throw, rate-limit key assertion) | ➖ None needed |
| 7.4 | `application/map-get-article-engagement-summary-result.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 3 cases (one per non-ok result kind) | ➖ None needed |
| 7.5 | `ui/use-engagement.test.ts` | Unit (hook) | N/A (new) | ✅ Written | ✅ Passed | ✅ 9 cases (summary success/failure, optimistic increment, no-double-increment, localStorage restore, throwing getItem, cancelled-unmount, view/reaction POST network failures) | ➖ None needed |
| 7.5 | `ui/reaction-button.test.tsx` | Unit (component) | N/A (new) | ✅ Written | ✅ Passed | ✅ 3 cases (no-count state, click, active state) | ➖ None needed |
| 7.5 | `ui/engagement-panel.test.tsx` | Unit (component) | N/A (new) | ✅ Written | ✅ Passed | ✅ 3 cases (degraded state, one-view-POST, optimistic click) | ➖ None needed |
| 7.5 | `blog/ui/article-page.test.tsx` | Unit (component) | ✅ 4/4 | ✅ Written (children-composition behavior) | ✅ Passed | ✅ 1 case (no-children omission) | ➖ None needed |

#### Test Summary (PR7)
- **Total tests written**: 55 (4 derive-visitor-hash + 2 mdx-article-slug-checker + 5 view-request-schema + 7 record-article-view + 6 map-engagement-write-result + 5 reaction-request-schema + 7 record-article-reaction + 6 get-article-engagement-summary + 4 map-get-article-engagement-summary-result + 10 use-engagement + 4 reaction-button + 4 engagement-panel + 2 article-page net-new)
- **Total tests passing**: 340/340 unit+integration (up from 272/272), 32/32 e2e (unchanged from PR6 — no new e2e spec added, task 7 has no dedicated Playwright task; the existing `e2e/blog.spec.ts` already exercises the mounted `EngagementPanel` firing a real view POST against Docker Postgres as a side effect of loading the article page)
- **Layers used**: Unit 51 (incl. 8 component/hook tests), Integration 0 new (PR5b's repository integration tests are reused unchanged), E2E 0 new
- **Approval tests** (refactoring): None
- **Pure functions created**: `deriveVisitorHash`, `mapEngagementWriteResultToResponse`, `mapGetArticleEngagementSummaryResultToResponse` — 3; `recordArticleView`/`recordArticleReaction`/`getArticleEngagementSummary` are intentionally impure (I/O orchestration), `useEngagement`/`react`/`hasReacted` are intentionally impure (client state + network), per strict-tdd.md's "don't force it where it doesn't fit"

#### Verification (PR7)
- `npm run typecheck` — clean.
- `npm run lint` — clean (0 errors, 0 new eslint-boundaries edges needed).
- `npm run test:coverage` — **340/340 tests passing** (up from 272/272), coverage **98.30% / 93.98% / 98.43% / 98.28%** (stmts/branches/funcs/lines — up from 97.88/93.77/98.18/97.85 on all four metrics, no regression).
- `npm run build` — 19 routes (up from 16): adds `/api/engagement/views`, `/api/engagement/reactions`, `/api/engagement/[slug]` (all dynamic, as expected for route handlers); the 16 prior routes are unchanged.
- `npm run test:e2e` — **32/32 passing** (unchanged from PR6), run against a real local Docker Postgres container (`compose.yaml`, migrated via `npm run db:migrate`) and a genuine `next build` + `next start` production server. First run showed 3 spurious `contact.spec.ts` failures caused by a stale pre-existing dev server on port 3000 (see findings above, not a PR7 regression); re-run with `CI=1` (forces a fresh server) confirmed a clean 32/32.

#### Review budget (PR7)
- **2021 hand-written changed lines (2018 insertions / 3 deletions)** — measured via `git diff --stat feat/pr6-contact...feat/pr7-engagement -- . ':!package-lock.json'` (`package-lock.json` untouched — zero new dependencies this PR). Well above the 400-line budget, disclosed honestly per `feature-branch-chain`, consistent with every PR in this chain since PR3a. Breakdown: engagement `application` layer (3 use-cases + 2 request schemas + 2 response mappers + the shared result type) + tests (~800 lines), engagement `ui` (hook + 2 components) + tests (~750 lines, the largest single layer — `use-engagement.test.ts` alone is ~300 lines covering 10 distinct network/storage/lifecycle scenarios), 1 domain port + 1 infrastructure implementation + tests (~75 lines), 3 thin `app/api/engagement/*` route handlers (~140 lines, excluded from the coverage gate like every prior route handler), `ArticlePage`/article-route composition-root wiring (~25 lines), `shared/security/derive-visitor-hash.ts` + test (~65 lines), i18n message catalogs (~20 lines). Atomic-unit justification: view counting, reactions, the public summary read, and the UI that drives all three share one `ArticleSlugChecker` port, one `EngagementWriteResult`/mapper pair, and one client hook — splitting further (e.g. views-only vs. reactions-only vs. UI-only PRs) would force re-justifying and re-reviewing the identical slug-validation/rate-limit/dedupe orchestration three or four times for no independently-shippable value, the same reasoning as every prior above-budget PR in this chain (PR2a/PR2b/PR3a/PR4/PR3b/PR5b/PR6).

### PR8 apply findings (2026-07-07)

- **`ArticleSearchRepository`'s `reconcile()` does both halves of "full reconcile" in one call, not separate upsert/prune methods** — matches the search spec's explicit "an upsert-only sync that never prunes does not satisfy this requirement": a single method makes it structurally impossible for `scripts/sync-search.ts` to call upsert without also pruning. Pruning is diffed in JS (fetch existing (slug, locale) keys, delete whatever isn't in the current entry set) rather than a single SQL `(slug, locale) NOT IN (...)` composite-tuple clause — postgres.js/Drizzle tuple-IN parameter binding is fragile and unfamiliar territory in this codebase; the two-query diff is simple to read, simple to test, and (with a handful of articles) not a real performance concern.
- **`search_vector` refresh uses a bound-parameter `::regconfig` cast, not a raw string-interpolated regconfig** — `setweight(to_tsvector(${regconfig}::regconfig, ${title}), 'A') || ...` with Drizzle's `sql` tag automatically parameterizes `${regconfig}`/`${title}`/etc.; Postgres accepts the explicit cast from a bound `text` parameter to `regconfig` (verified empirically against pglite in `drizzle-article-search-repository.test.ts`, including a query containing `to_tsquery`-special characters `& | :` via `websearch_to_tsquery`, per the search spec's "Query with special characters does not error" scenario). No fallback needed — this is the same pglite/real-Postgres parity PR5b's escape hatch verified in principle, now exercised for real by the first actual `to_tsvector`/`websearch_to_tsquery`/`ts_rank` queries in this codebase.
- **Regconfig mapping (`regconfigForLocale`) is the first `domain` file in this project with real branching logic — and it exposed a genuine eslint-boundaries gap**: every prior `domain` file (repository interfaces, `ArticleSlugChecker`) was purely structural with triangulation explicitly skipped; `regconfigForLocale`'s own co-located unit test needed to import its sibling production file, both classified as `domain` in the same feature — and `domain: allow: []` has no same-feature carve-out (unlike `application`/`infrastructure`/`ui`, each of which already has one, added in PR3a/PR4). Fixed by adding the missing same-feature `domain -> domain` capture-scoped edge to `eslint.config.mjs`, the same "fix the boundary when a real, legitimate need surfaces" precedent as every prior boundary gap found in this chain. Cross-feature `domain -> domain` stays disallowed by the capture match.
- **Request-shape Zod schema lives in `application`, not `domain`, despite design.md's literal "domain: ... Zod domain schemas" wording** — `search-query-schema.ts` follows the established precedent from `contact`/`engagement` (`contact-submission-schema.ts`, `view-request-schema.ts`, `reaction-request-schema.ts`), all of which validate HTTP request shape as part of application-layer orchestration, not domain entities. Consistency with existing code wins over the literal design.md phrase here.
- **Search UI is split into "input only" (search/ui) + "results rendering" (home/ui), not one self-contained widget** — the home-page spec requires the search input to sit *above* the category filter pills while its *results* replace the *list area below* the pills (two different DOM regions, not one bundled widget). `ArticleSearchInput` (search/ui) therefore renders only the `<input>` and reports fetched results upward via an `onStateChange` callback; `ArticlesSection` (home/ui) renders the actual result rows in its own list area, using a new home-owned `HomeSearchResultSummary` domain type (mirrors `HomeArticleSummary`) rather than importing `search`'s result type.
- **A genuinely new composition pattern was needed beyond PR6/PR7's children-slot precedent**: `ContactSection`/`ArticlePage`'s children-composition works because the injected widget is *self-contained and one-directional* (home renders it, done). Search needs *bidirectional* coordination — typing must deactivate the category pill (search -> home), and selecting a pill must reset the search widget's own internal text (home -> search) — which a plain `children` prop can't express, and which a Server Component (`page.tsx`) can't bridge with a function prop across the RSC boundary (functions aren't serializable from Server to Client Components, only rendered JSX is). Resolved with a new small Client Component, `app/[locale]/_components/articles-with-search.tsx` (an underscore-prefixed, non-route `app/` folder), which imports both `home/ui` and `search/ui` directly and owns the bridging state — legal because `app/*` may import any feature's `ui` unrestricted, and because everything below that wrapper is plain client-side React (no RSC boundary issues once inside a `"use client"` subtree).
- **Two genuinely unreachable defensive branches found and removed by coverage, not invented workarounds** (same category as PR5b's `ArticleViewRepository.countViews` and PR7's `isReady` derivation): (1) `ArticleSearchInput` originally stored `status` as independent state with a `setStatus("idle")` call inside an effect whenever `query` became empty — flagged by `react-hooks/set-state-in-effect`; fixed by deriving `status` from `query.length === 0 ? "idle" : fetchStatus` instead of storing it, which is *also* more correct (an empty query deterministically implies idle, it isn't information that needs its own state slot). (2) the debounce effect's cleanup function guarded `if (debounceRef.current) clearTimeout(...)`, but that cleanup is only ever registered *after* `debounceRef.current` was just assigned a few lines above — the guard's false branch is structurally unreachable; simplified to an unconditional `clearTimeout(debounceRef.current!)`.
- **No separate deploy workflow exists in this repo** — design.md's "migrate -> sync-search -> deploy promote" pipeline order is realized here only via `.github/workflows/ci.yml`'s e2e job (`Run database migrations` then `Sync search index`, now `npm run db:sync-search` instead of the prior no-op placeholder); there is no actual deploy-to-production workflow file to wire a third "promote" step into yet. `npm run db:sync-search` (`tsx scripts/sync-search.ts`) is the concrete script a future deploy workflow would call.
- **Manual verification against real Postgres**: `docker compose up` was already running from a prior session; `npm run db:migrate` + `npm run db:sync-search` reconciled all 7 real (slug, locale) rows from `content/blog/` (3 bilingual articles + the es-only `notas-breves` fixture from PR4), confirmed via `npm run build` (20 routes, up from 19 — adds `GET /api/search`) and a full Playwright run against `next start`.
- **Stale dev server on port 3000 checked proactively this time** — `lsof -i :3000` before the e2e run found a leftover Node process from an earlier, unrelated session (same recurring hazard class as PR3a/PR7's documented findings); killed it and ran with `CI=1` to force a fresh production `webServer`, avoiding a repeat of the spurious-failure pattern.
- **Coverage**: 97.79%/94.00%/97.35%/97.75% (stmts/branches/funcs/lines) — branch coverage *exceeds* PR7's 93.98% baseline; statements/functions/lines are marginally below PR7's 98.30/98.43/98.28 (by 0.5-1.1 points), entirely attributable to `scripts/sync-search.ts`'s CLI dispatch guard (`main()`'s default `console.log`/`console.error`/`process.exit` bindings, and the top-level `if (isMainModule) { void main(); }` line) — this is the first CLI-entrypoint file in the project, and that specific guard genuinely cannot be exercised without invoking a real `process.exit` under the test runner's own process; `main()`'s actual success/failure logic is fully tested via dependency injection (`runner`/`io` params), leaving only ~4 lines structurally untestable. Every other file in this PR (`article-search-input.tsx`, `drizzle-article-search-repository.ts`, all application/domain files) is at 100% or effectively-100% (real-branch) coverage.

#### TDD Cycle Evidence (PR8)

| Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
|------|-----------|-------|------------|-----|-------|-------------|----------|
| 8.1 | `scripts/sync-search/strip-markdown-to-text.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 10 cases (headings, bold/italic, links, images, code fences, inline code, JSX tags, lists, blockquotes, whitespace) | ✅ Fixed a real bug (JSX-tag regex matching a TS generic like `Promise<Article \| null>` inside a code fence) via a code-block placeholder swap |
| 8.1 | `src/features/search/domain/regconfig-for-locale.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 2 cases (es->spanish, en->english — both branches) | ➖ None needed |
| domain (8.1) | `article-search-repository.ts` | N/A | N/A (new) | N/A | N/A | Triangulation skipped: purely structural interface/type exports, zero runtime behavior, zero-import enforced by eslint boundaries `domain: allow: []` | N/A |
| 8.1 | `scripts/sync-search/collect-search-entries.test.ts` | Unit (fake loader) | N/A (new) | ✅ Written | ✅ Passed | ✅ 3 cases (builds entries + strips markdown, skips missing-locale files, empty content tree) | ➖ None needed |
| 8.1 | `drizzle-article-search-repository.test.ts` | Integration (pglite) | N/A (new) | ✅ Written | ✅ Passed | ✅ 7 cases (upsert+search, locale filtering, relevance ranking, no-match, special characters, prune, idempotent re-run) | ➖ None needed |
| 8.1 | `scripts/sync-search/run-sync-search.test.ts` | Unit (fakes) | N/A (new) | ✅ Written | ✅ Passed | ✅ 2 cases (real content, empty/full-prune case) | ➖ None needed |
| 8.1 | `scripts/sync-search.test.ts` | Unit (mocked db/repo, real loader) | N/A (new) | ✅ Written | ✅ Passed | ✅ 3 cases (real content-tree wiring, `main()` success, `main()` failure) | ✅ Extracted testable `main()` from the untestable CLI guard |
| 8.2 | `search-query-schema.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 6 cases (valid, empty, oversized, boundary 200, invalid locale, missing locale) | ➖ None needed |
| 8.2 | `search-articles.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 7 cases (ok, rate-limited, invalid query, invalid locale, unavailable, no-match ok, endpoint-key assertion) | ➖ None needed |
| 8.2 | `map-search-articles-result.test.ts` | Unit | N/A (new) | ✅ Written | ✅ Passed | ✅ 5 cases (one per result kind + zero-results) | ➖ None needed |
| 8.3 | (covered by `drizzle-article-search-repository.test.ts` + `search-articles.test.ts` above — ranking, no-match, DB-unavailable) | Integration + Unit | — | — | — | — | — |
| 8.4 | `article-search-input.test.tsx` | Unit (component) | N/A (new) | ✅ Written | ✅ Passed | ✅ 12 cases (renders, debounce, ok results, clear-to-idle, 503 disables, non-503 error, network-reject error, rapid-typing cancels pending fetch, reset-while-pending cancels fetch, reset-before-any-typing, reset clears query+state, unmount-before-typing) | ✅ Derived `status` instead of storing it (avoided `set-state-in-effect`); removed an unreachable `if (debounceRef.current)` guard in the cleanup function |
| 8.4 | `articles-section.test.tsx` | Unit (component) | ✅ 6/6 | ✅ Written (new controlled-prop behavior) | ✅ Passed | ✅ 4 new cases (search slot renders, search results replace list + deactivate a selected pill, search empty state, onSearchReset fires only when a search is active) | ➖ None needed |
| 8.4 | `e2e/search.spec.ts` | E2E | N/A (new) | ✅ Written | ✅ Passed (against real Docker Postgres, after `db:migrate` + `db:sync-search`) | ✅ 4 cases (deactivates a selected pill, clears back to "Todos", pill-select clears search, no-match empty state) | ➖ None needed |

#### Test Summary (PR8)
- **Total tests written**: 68 (10 strip-markdown + 2 regconfig + 3 collect-entries + 7 drizzle-search-repo + 2 run-sync-search + 3 sync-search-cli + 6 search-query-schema + 7 search-articles + 5 map-search-articles-result + 12 article-search-input + 4 net-new articles-section + 4 e2e)
- **Total tests passing**: 403/403 unit+integration (up from 340/340 at PR7 — note: PR7's own baseline count of 340 already included the 55 tests it added that PR; PR8 adds 68 net-new: some PR8 test files replace/extend pre-existing `articles-section.test.tsx` cases rather than all being brand new files), 36/36 e2e (up from 32/32 — 4 new search scenarios)
- **Layers used**: Unit 58 (incl. 12 component tests), Integration 7 (pglite), E2E 4
- **Approval tests** (refactoring): 1 — `articles-section.test.tsx`'s existing 6 cases re-run unchanged as a safety net before adding the 4 new controlled-prop cases (no behavior regression in the pre-PR8 category-filter/empty-state paths)
- **Pure functions created**: `stripMarkdownToText`, `regconfigForLocale`, `collectSearchEntries` — 3; `createDrizzleArticleSearchRepository`/`searchArticles`/`runSyncSearch`/`main` are intentionally impure (I/O orchestration), `ArticleSearchInput`/`ArticlesWithSearch` are intentionally impure (client state + network), per strict-tdd.md's "don't force it where it doesn't fit"

#### Verification (PR8)
- `npm run typecheck` — clean.
- `npm run lint` — clean (0 errors after the `domain -> domain` same-feature boundaries addition; 2 harmless `no-console` eslint-disable warnings removed since that rule isn't enabled).
- `npm run test:coverage` — **403/403 tests passing** (up from 340/340), coverage **97.79% / 94.00% / 97.35% / 97.75%** (stmts/branches/funcs/lines — branches *exceed* PR7's 93.98%; stmts/funcs/lines marginally below PR7's 98.30/98.43/98.28, fully attributable to `scripts/sync-search.ts`'s irreducible CLI-guard lines, see findings above).
- `npm run build` — 20 routes (up from 19): adds `GET /api/search` (dynamic route handler, as expected).
- `npm run test:e2e` — **36/36 passing** (up from 32/32), run against a real local Docker Postgres container (`compose.yaml`, migrated via `npm run db:migrate`, indexed via `npm run db:sync-search`) and a genuine `next build` + `next start` production server, with `CI=1` after proactively checking for (and finding/killing) a stale dev server on port 3000.

#### Review budget (PR8)
- **2047 hand-written changed lines (2033 insertions / 14 deletions)** — measured via `git diff --stat feat/pr7-engagement...feat/pr8-search -- . ':!package-lock.json'` (`package-lock.json` untouched — zero new dependencies this PR; `tsx` was already a devDependency since PR1). Well above the 400-line budget, disclosed honestly per `feature-branch-chain`, consistent with every PR in this chain since PR3a. Breakdown: `scripts/sync-search/*` + CLI entry + tests (~430 lines), `search` domain + infrastructure + tests (~340 lines, incl. the pglite integration suite verifying real `websearch_to_tsquery`/`ts_rank`/regconfig parity), `search` application layer + route handler + tests (~460 lines), `search/ui`'s `ArticleSearchInput` + test (~380 lines, the single largest file — 12 distinct network/timer/lifecycle scenarios), `home/ui`'s `ArticlesSection` refactor + test additions + the new `HomeSearchResultSummary` domain type (~240 lines), the new `ArticlesWithSearch` composition-root wrapper (~65 lines), `e2e/search.spec.ts` (~90 lines), i18n message catalogs + `eslint.config.mjs`/`package.json`/`ci.yml`/tasks.md/state.yaml bookkeeping (~40 lines). Atomic-unit justification: the sync script's full-reconcile shape, the repository it dictates, the search endpoint's rate-limit/validation orchestration, and the UI that renders results in place are one deliverable — a real end-to-end search feature cannot be reviewed or verified with any one of those pieces missing, the same reasoning as every prior above-budget PR in this chain (PR2a/PR2b/PR3a/PR4/PR3b/PR5b/PR6/PR7).
