/**
 * The GitHub account whose public activity this section surfaces — this
 * is Andrés Valencia's own portfolio, so the username is a fixed value
 * (github-activity: Server-Side GitHub Data Fetch), not user-configurable
 * input. Same account already hardcoded across `features/home/ui`'s
 * hero/contact/projects sections (`github.com/andresvalencia72`).
 */
export const GITHUB_USERNAME = "andresvalencia72";
