# Always loaded (both before and after the TLS bootstrap, see
# infra/README.md) — kept in its own file so it isn't duplicated between
# default.ssl.conf (the ACTIVE, committed TLS config) and
# default.http-bootstrap.conf.disabled (the fresh-server-only, normally
# inactive HTTP config).
upstream app_upstream {
    server app:3000;
}
